diff -ruN --no-dereference proj_p103/confidence_calculation/docker-compose-crypto-orderflow.yml proj_p104/confidence_calculation/docker-compose-crypto-orderflow.yml
--- proj_p103/confidence_calculation/docker-compose-crypto-orderflow.yml	2026-02-25 19:11:39.511191784 +0000
+++ proj_p104/confidence_calculation/docker-compose-crypto-orderflow.yml	2026-02-25 19:29:13.352590846 +0000
@@ -2935,6 +2935,9 @@
       - DYN_CFG_KEY=settings:dynamic_cfg
       # Fail-closed hold (align with other anti-flap defaults)
       - AUTO_APPLY_BLOCK_HOLD_S=300
+      - PROM_RULES_BUNDLE_SMOKE_BLOCK_REASON=prom_rules_bundle_smoke
+      - PROM_RULES_BUNDLE_SMOKE_PROMTOOL=auto
+      - PROM_RULES_REPO_ROOT=/app
       # Orchestrated module
       - ENABLE_PROM_RULES_BUNDLE_SMOKE=1
     depends_on: *id003
diff -ruN --no-dereference proj_p103/orderflow_services/prometheus_rules_bundle_manifest_v1.yml proj_p104/orderflow_services/prometheus_rules_bundle_manifest_v1.yml
--- proj_p103/orderflow_services/prometheus_rules_bundle_manifest_v1.yml	2026-02-25 14:55:22.000000000 +0000
+++ proj_p104/orderflow_services/prometheus_rules_bundle_manifest_v1.yml	2026-02-25 19:27:54.140201988 +0000
@@ -1,23 +1,4 @@
-# Prometheus rules bundle manifest (v1)
-#
-# Purpose
-#   Single include-list for deployment of Prometheus rule files.
-#
-# Typical usage
-#   1) Copy repo directories into your Prometheus rules dir (example):
-#        /etc/prometheus/rules/<repo_root>/...
-#   2) In prometheus.yml, set rule_files to these globs.
-#
-# Notes
-#   - This intentionally uses globs so new rule files are auto-included.
-#   - Keep the rules directory layout stable.
-
 rule_file_globs:
   - orderflow_services/prometheus_alerts_*.yml
-  - ok_rate_logic/prometheus_alerts_*.yml
   - services/orderflow/prometheus_alerts_*.yml
-  - file: orderflow_services/prometheus_alerts_of_inputs_dlq_p96.yml
-    tier: "P96"
-    impact: "High"
-    owner: "SRE"
-- prometheus_alerts_policy_effectiveness_p71.yml
+  - ok_rate_logic/prometheus_alerts_*.yml
diff -ruN --no-dereference proj_p103/services/prom_rules_bundle_smoke_runner_v1.py proj_p104/services/prom_rules_bundle_smoke_runner_v1.py
--- proj_p103/services/prom_rules_bundle_smoke_runner_v1.py	2026-02-25 19:11:39.414701397 +0000
+++ proj_p104/services/prom_rules_bundle_smoke_runner_v1.py	2026-02-25 19:28:48.978006499 +0000
@@ -1,84 +1,162 @@
-"""Prometheus rules bundle repo validator smoke runner (P102).
+from __future__ import annotations
 
-Goal:
-- Validate that all Prometheus rule files referenced by the repository bundle are parseable.
-- Fail-closed by setting a global auto-apply block key when validation fails.
-
-This is designed to be executed from an orchestrator (docker-compose timer / systemd).
-
-Exit codes:
-- 0: OK
-- 2: Validation failed (intentional signal for orchestrator/alerts)
-- 1: Unexpected error
-"""
+"""Prometheus rules bundle smoke runner (standalone timer).
 
-from __future__ import annotations
+Why this exists
+- Some deployments run `services.of_timers_worker` (which already has a prom-rules
+  smoke-check), but others may prefer a dedicated timer container.
+
+What it does
+- Executes `orderflow_services.prom_rules_bundle_health_check_v1` (syntax + optional
+  promtool validation) against the repo rules bundle.
+- On failure: sets a fail-closed auto-apply block key (legacy guard key) for a
+  bounded hold time, so config/model auto-apply is paused.
+- On success: clears the block it previously set.
+
+Exit codes
+- 0 OK
+- 2 validation failed (or cannot run)
+
+ENV
+- REDIS_URL (default: redis://redis-worker-1:6379/0)
+- PROM_RULES_REPO_ROOT (optional; default auto-detect in health-check)
+- PROM_RULES_BUNDLE_SMOKE_PROMTOOL=auto|on|off (default: auto)
+
+Auto-apply block
+- AUTO_APPLY_BLOCK_PREFIX (default: cfg:suggestions:entry_policy:auto_apply_block)
+- PROM_RULES_BUNDLE_SMOKE_BLOCK_REASON (default: prom_rules_bundle_smoke)
+- AUTO_APPLY_BLOCK_HOLD_S (default: 300)
+
+Block keys written
+- {prefix}:{reason} = 1
+- {prefix}:{reason}:ts_ms
+- {prefix}:{reason}:meta  (small JSON)
+"""
 
+import json
 import os
-import sys
 import time
-import traceback
-
-from orderflow_services.validate_prometheus_rules_bundle_v1 import validate_prometheus_rules_bundle
+from typing import Any, Dict, Optional
 
 try:
     import redis  # type: ignore
 except Exception:  # pragma: no cover
     redis = None
 
-AUTO_APPLY_BLOCK_PREFIX = "cfg:suggestions:entry_policy:auto_apply_block"
+from orderflow_services.prom_rules_bundle_health_check_v1 import main as health_main
 
 
-def _redis_client():
+def _now_ms() -> int:
+    return int(time.time() * 1000)
+
+
+def _connect_redis() -> Optional["redis.Redis"]:
     if redis is None:
-        raise RuntimeError("redis python package is required")
+        return None
+    url = (os.getenv("REDIS_URL") or os.getenv("CRYPTO_NOTIFY_REDIS_URL") or "redis://redis-worker-1:6379/0").strip()
+    try:
+        return redis.Redis.from_url(url, decode_responses=True)
+    except Exception:
+        return None
 
-    redis_url = os.environ.get("REDIS_URL")
-    if not redis_url:
-        raise RuntimeError("REDIS_URL is required")
-
-    # decode_responses=True so values are strings and logs are readable
-    return redis.Redis.from_url(redis_url, decode_responses=True)
-
-
-def _set_global_block(reason: str, hold_s: int = 300) -> None:
-    """Set a global fail-closed auto-apply block for the entry-policy promoter."""
-    r = _redis_client()
-    key = f"{AUTO_APPLY_BLOCK_PREFIX}:prom_rules_bundle:{reason}"
-    ts_ms = int(time.time() * 1000)
-    r.set(key, str(ts_ms), ex=max(60, int(hold_s)))
 
+def _block_keys(prefix: str, reason: str) -> tuple[str, str, str]:
+    k = f"{prefix}:{reason}"
+    return k, f"{k}:ts_ms", f"{k}:meta"
 
-def main() -> int:
-    repo_root = os.environ.get("PROM_RULES_REPO_ROOT", ".")
-    promtool = os.environ.get("PROMTOOL_PATH")  # optional
-    hold_s = int(os.environ.get("AUTO_APPLY_BLOCK_HOLD_S", "300"))
-
-    try:
-        ok, report = validate_prometheus_rules_bundle(
-            repo_root=repo_root,
-            promtool_path=promtool,
-        )
-        if ok:
-            print("OK: prom rules bundle validation")
-            return 0
-
-        # Fail-closed: set global auto-apply block so nothing is promoted automatically.
-        print("FAIL: prom rules bundle validation")
-        print(report)
-        _set_global_block(reason="invalid_rules", hold_s=hold_s)
-        return 2
 
+def _set_block(*, reason: str, meta: Dict[str, Any], hold_s: int) -> None:
+    r = _connect_redis()
+    if r is None:
+        return
+
+    prefix = (os.getenv("AUTO_APPLY_BLOCK_PREFIX") or "cfg:suggestions:entry_policy:auto_apply_block").strip()
+    k, k_ts, k_meta = _block_keys(prefix, reason)
+
+    now = _now_ms()
+
+    pipe = r.pipeline(transaction=False)
+    pipe.set(k, "1", ex=int(hold_s))
+    pipe.set(k_ts, str(now), ex=int(hold_s))
+
+    try:
+        meta_blob = json.dumps(meta, ensure_ascii=False, separators=(",", ":"))
+    except Exception:
+        meta_blob = ""
+    if meta_blob:
+        pipe.set(k_meta, meta_blob, ex=int(hold_s))
+
+    try:
+        pipe.execute()
     except Exception:
-        print("ERROR: prom rules bundle smoke runner crashed")
-        traceback.print_exc()
+        return
+
+
+def _clear_block_if_owned(*, reason: str) -> None:
+    r = _connect_redis()
+    if r is None:
+        return
+
+    prefix = (os.getenv("AUTO_APPLY_BLOCK_PREFIX") or "cfg:suggestions:entry_policy:auto_apply_block").strip()
+    k, k_ts, k_meta = _block_keys(prefix, reason)
+
+    try:
+        meta_blob = r.get(k_meta) or ""
+        if meta_blob:
+            meta = json.loads(meta_blob)
+            if isinstance(meta, dict) and meta.get("owner") != "prom_rules_bundle_smoke_runner_v1":
+                # do not delete blocks set by other components
+                return
+    except Exception:
+        # if meta cannot be parsed, avoid deleting someone else's key
+        return
+
+    try:
+        r.delete(k, k_ts, k_meta)
+    except Exception:
+        return
+
+
+def main() -> int:
+    repo_root = (os.getenv("PROM_RULES_REPO_ROOT") or "").strip() or None
+    promtool_mode = (os.getenv("PROM_RULES_BUNDLE_SMOKE_PROMTOOL") or "auto").strip().lower()
+    if promtool_mode not in ("auto", "on", "off"):
+        promtool_mode = "auto"
+
+    block_reason = (os.getenv("PROM_RULES_BUNDLE_SMOKE_BLOCK_REASON") or "prom_rules_bundle_smoke").strip() or "prom_rules_bundle_smoke"
+    hold_s = int(os.getenv("AUTO_APPLY_BLOCK_HOLD_S", "300"))
+
+    argv = []
+    if repo_root:
+        argv += ["--root", repo_root]
+    argv += ["--promtool", promtool_mode]
+
+    rc = 2
+    try:
+        rc = int(health_main(argv))
+    except SystemExit as e:
         try:
-            _set_global_block(reason="runner_crash", hold_s=hold_s)
+            rc = int(e.code) if e.code is not None else 2
         except Exception:
-            # Don't mask original error
-            pass
-        return 1
+            rc = 2
+    except Exception:
+        rc = 2
+
+    if rc == 0:
+        _clear_block_if_owned(reason=block_reason)
+        return 0
+
+    _set_block(
+        reason=block_reason,
+        meta={
+            "owner": "prom_rules_bundle_smoke_runner_v1",
+            "ts_ms": _now_ms(),
+            "kind": "prom_rules_bundle_smoke",
+        },
+        hold_s=hold_s,
+    )
+    return 2
 
 
 if __name__ == "__main__":
-    sys.exit(main())
+    raise SystemExit(main())
diff -ruN --no-dereference proj_p103/tick_flow_full/orderflow_services/prometheus_rules_bundle_manifest_v1.yml proj_p104/tick_flow_full/orderflow_services/prometheus_rules_bundle_manifest_v1.yml
--- proj_p103/tick_flow_full/orderflow_services/prometheus_rules_bundle_manifest_v1.yml	2026-02-25 14:55:22.000000000 +0000
+++ proj_p104/tick_flow_full/orderflow_services/prometheus_rules_bundle_manifest_v1.yml	2026-02-25 19:28:00.247338574 +0000
@@ -1,9 +1,3 @@
-# Prometheus rules bundle manifest (v1) — tick_flow_full mirror
-#
-# Use when you deploy rules from the tick_flow_full subtree only.
-# (Paths are relative to tick_flow_full/)
-
 rule_file_globs:
   - orderflow_services/prometheus_alerts_*.yml
   - services/orderflow/prometheus_alerts_*.yml
-- prometheus_alerts_policy_effectiveness_p71.yml
