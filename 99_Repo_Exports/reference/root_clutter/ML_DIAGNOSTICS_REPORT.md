# ML Diagnostics Report

**Дата:** 2026-02-03  
**Время выполнения:** Пошаговая диагностика ML rollout FREEZE

---

## 📋 Шаг 1: Проверка конфигурации ML

### Результат:
```
❌ Конфигурация не найдена в Redis key: cfg:ml_confirm
```

### Выводы:
- **КРИТИЧЕСКАЯ ПРОБЛЕМА:** Конфигурация ML полностью отсутствует
- Redis key `cfg:ml_confirm` пустой или не существует
- Это объясняет 100% ошибок - модель не может работать без конфигурации

### Что должно быть в конфигурации:
- `mode`: OFF/SHADOW/ENFORCE
- `model_path`: путь к файлу модели (.joblib)
- `meta_path`: путь к метаданным модели (.json)
- `model_ver`: версия модели
- `enforce_share`: доля трафика для ENFORCE (0.0-1.0)
- `fail_policy`: OPEN/CLOSED
- `p_min_default`: минимальный порог вероятности

---

## 📊 Шаг 2: Диагностика ошибок

### Результат:
```
❌ Нет данных в stream metrics:ml_confirm за последние 60 минут
```

### Анализ данных за 24 часа:
- **Всего записей:** 991
- **С ошибками (status ERR):** 991 (100%)
- **Режим работы:** SHADOW (все записи)
- **Поле error:** пустое (ошибки в status, не в error)

### Выводы:
1. **Все запросы возвращают статус ERR** - это `ERR_NO_CFG` (нет конфигурации)
2. **Режим SHADOW** означает, что ML не блокирует сигналы, только логирует
3. **Ошибки не в поле error**, а в `status: ERR_NO_CFG`
4. **Нет свежих данных** за последние 60 минут - возможно, система не работает или метрики не пишутся

### Типичная ошибка:
```
status: ERR_NO_CFG
mode: SHADOW
p_edge: 0.0
```

---

## ⏱️ Шаг 3: Диагностика латентности

### Результат:
```
❌ Нет данных в stream metrics:ml_confirm за последние 60 минут
```

### Выводы:
- Невозможно проанализировать латентность без свежих данных
- Старые данные показывают `lat_p99: 0.172` (172 мс) из сообщения guard
- Высокая латентность может быть связана с ошибками (быстрый fail при отсутствии конфигурации)

---

## 📋 Шаг 4: Список pending предложений guard

### Результат:
Найдено **2 pending предложения** для FREEZE:

#### Предложение #1:
- **Bundle ID:** `53e81d51740b`
- **Время:** 2026-02-03 19:50:38
- **Метрики:**
  - `n: 2407.0` (достаточно данных)
  - `err_rate: 1.0` (100% ошибок) ⚠️
  - `lat_p99: 0.172` (172 мс, порог: 6 мс) ⚠️
  - `p50: 0.0` (медиана p_edge = 0) ⚠️
- **Предлагаемое изменение:**
  - `enforce_share: 1.0 → 0.05` (100% → 5%)

#### Предложение #2:
- **Bundle ID:** `0791fe744b7b`
- **Время:** 2026-02-03 20:01:05
- **Метрики:**
  - `n: 446.0`
  - `err_rate: 1.0` (100% ошибок) ⚠️
  - `lat_p99: 0.578` (578 мс, порог: 6 мс) ⚠️
  - `p50: 0.0`
- **Предлагаемое изменение:**
  - `enforce_share: 1.0 → 0.05` (100% → 5%)

---

## 🔍 Корневая причина проблем

### Основная проблема:
**Конфигурация ML отсутствует** (`cfg:ml_confirm` пустая)

### Цепочка проблем:
1. ❌ Конфигурация не настроена → `cfg:ml_confirm` пустая
2. ❌ ML gate не может загрузить модель → `ERR_NO_CFG`
3. ❌ 100% запросов возвращают ошибку → `err_rate: 1.0`
4. ❌ Высокая латентность из-за быстрого fail → `lat_p99: 172-578 мс`
5. ❌ Guard обнаружил проблемы → предложил FREEZE

---

## ✅ Рекомендации по исправлению

### 1. Настроить конфигурацию ML (КРИТИЧНО)

```bash
# Проверить существование файлов модели
docker exec scanner-signal-parser-worker ls -lh /app/models/ml_confirm/ 2>/dev/null || echo "Модели не найдены"

# Установить базовую конфигурацию
docker exec redis-worker-1 redis-cli HSET cfg:ml_confirm \
  mode SHADOW \
  fail_policy OPEN \
  enforce_share 0.05 \
  p_min_default 0.55
```

### 2. Если модель существует, добавить пути:

```bash
docker exec redis-worker-1 redis-cli HSET cfg:ml_confirm \
  model_path /app/models/ml_confirm/model.joblib \
  meta_path /app/models/ml_confirm/meta.json \
  model_ver v1.0
```

### 3. После настройки конфигурации:

```bash
# Перезапустить worker для перезагрузки модели
docker restart scanner-signal-parser-worker

# Подождать несколько минут и проверить метрики
python3 python-worker/tools/ml_diagnose_errors.py --window-min 5
```

### 4. Управление предложениями guard:

**Вариант A: Подтвердить FREEZE (рекомендуется)**
```bash
# После понимания проблемы - подтвердить снижение до 5%
python3 python-worker/tools/ml_guard_approve.py --action confirm --bundle-id 53e81d51740b
```

**Вариант B: Отклонить (если проблемы исправлены)**
```bash
# Если конфигурация восстановлена и модель работает
python3 python-worker/tools/ml_guard_approve.py --action reject --bundle-id 53e81d51740b
```

---

## 📈 Ожидаемые результаты после исправления

1. ✅ Конфигурация настроена → `cfg:ml_confirm` заполнена
2. ✅ Модель загружена → `status: OK` или `SHADOW`
3. ✅ Ошибки снижаются → `err_rate < 0.01` (1%)
4. ✅ Латентность нормализуется → `lat_p99 < 6 мс`
5. ✅ Guard предложит разморозку после 7 дней хороших метрик

---

## 🎯 Итоговые выводы

### Критические проблемы:
1. ❌ **Конфигурация ML отсутствует** - основная причина всех проблем
2. ❌ **100% ошибок** - все запросы возвращают `ERR_NO_CFG`
3. ❌ **Высокая латентность** - превышает порог в 28-96 раз (172-578 мс vs 6 мс)

### Статус системы:
- 🔴 **КРИТИЧЕСКИЙ** - ML не работает из-за отсутствия конфигурации
- 🔴 **FREEZE предложен** - guard правильно обнаружил проблемы
- 🟡 **SHADOW режим** - система не блокирует сигналы, только логирует

### Следующие шаги:
1. ✅ Настроить конфигурацию ML (`cfg:ml_confirm`)
2. ✅ Убедиться, что файлы модели существуют
3. ✅ Перезапустить worker
4. ✅ Подтвердить FREEZE предложение (снизить до 5%)
5. ✅ Мониторить метрики после исправления
6. ✅ После стабилизации - разморозить (через 7 дней или вручную)

---

**Следующий запуск диагностики:**
```bash
./run_ml_diagnostics.sh
```

