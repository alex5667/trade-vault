services:
  go-worker-1m:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-1m
    stop_grace_period: 30s
    env_file:
    - .env
    - config/generated/symbols.env
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis
    - CROSSASSET_ENABLED=false
    - REDIS_PASSWORD=
    - REDIS_USERNAME=
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_1m
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2112
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=true
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_COINS=${HYPERLIQUID_COINS:-BTC,ETH}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - BYBIT_DATA_COLLECTION_ENABLED=false
    - BYBIT_FUTURES_WS_ENABLED=false
    - BYBIT_WS_PING_PERIOD=20s
    - BYBIT_WS_READ_TIMEOUT=90s
    - BYBIT_WS_HANDSHAKE_TIMEOUT=45s
    - BYBIT_WS_DIAL_TIMEOUT=30s
    - BYBIT_WS_TCP_KEEPALIVE=60s
    - BYBIT_WS_WRITE_WAIT=5s
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=15s
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=10s
    - FUTURES_WS_WRITE_WAIT=10s
    - LIQ_WS_ENABLED=true
    - LIQ_SOURCES=binance
    - BINANCE_FAPI_KEY=${BINANCE_API_KEY:-}
    - BINANCE_FAPI_SECRET=${BINANCE_API_SECRET:-}
    - V13_CROSSASSET_POLL_SEC=30
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2112:2112
    restart: unless-stopped
    command:
    - sh
    - -c
    - "echo \"\u23F3 \u041E\u0436\u0438\u0434\u0430\u043D\u0438\u0435 Redis (\u0443\
      \u0432\u0435\u043B\u0438\u0447\u0435\u043D\u043D\u043E\u0435 \u0432\u0440\u0435\
      \u043C\u044F)...\"\nsleep 5\necho \"\U0001F680 \u0417\u0430\u043F\u0443\u0441\
      \u043A go-worker-1m...\"\n./main\n"
    deploy:
      resources:
        limits:
          memory: 1.5G
          cpus: '1.5'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2112/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
  go-worker-5m:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-5m
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_5m
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2113
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2113:2113
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 15 && ./main
    deploy:
      resources:
        limits:
          memory: 1.2G
          cpus: '1.0'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2113/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
  go-worker-15m:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-15m
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_15m
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2114
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2114:2114
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 25 && ./main
    deploy:
      resources:
        limits:
          memory: 1.0G
          cpus: '0.8'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2114/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 270s
  go-worker-1h:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-1h
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_1h
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2115
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2115:2115
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 35 && ./main
    deploy:
      resources:
        limits:
          memory: 800M
          cpus: '0.6'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2115/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 360s
  go-worker-4h:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-4h
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_4h
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2116
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2116:2116
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 60 && ./main
    deploy:
      resources:
        limits:
          memory: 700M
          cpus: '0.5'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2116/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 450s
  go-worker-1d:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-1d
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_1d
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2117
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2117:2117
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 90 && ./main
    deploy:
      resources:
        limits:
          memory: 600M
          cpus: '0.4'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2117/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 540s
  go-worker-1w:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-1w
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_1w
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2118
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2118:2118
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 120 && ./main
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.3'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2118/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 630s
  go-worker-1month:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-1month
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_1M
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2119
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2119:2119
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 150 && ./main
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.3'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2119/monitoring/websocket/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 720s
  go-worker-3month:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-3month
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_3M
    - POLL_INTERVAL_HOURS=1
    - HTTP_PORT=2120
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2120
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2120:2120
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 180 && ./rest-fetcher
    deploy:
      resources:
        limits:
          memory: 400M
          cpus: '0.25'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2120/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 810s
  go-worker-1y:
    build:
      context: ./go-worker
      dockerfile: Dockerfile
    container_name: scanner-go-worker-1y
    stop_grace_period: 30s
    environment:
    - DRAIN_TIMEOUT_SEC=${DRAIN_TIMEOUT_SEC:-10}
    - BATCH_PUBLISHER_BACKPRESSURE_MS=${BATCH_PUBLISHER_BACKPRESSURE_MS:-10}
    - REDIS_HOST=redis-worker-1
    - REDIS_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_WORKER_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_USERNAME=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_USERNAME_2=${GO_WORKER_REDIS_USER}
    - REDIS_CANDLES_PASSWORD=${GO_WORKER_REDIS_PASS}
    - REDIS_CANDLES_PASSWORD_2=${GO_WORKER_REDIS_PASS}
    - REDIS_PORT=6379
    - REDIS_DB=0
    - REDIS_POOL_SIZE=15
    - REDIS_MIN_IDLE_CONNS=10
    - REDIS_MAX_RETRIES=5
    - REDIS_RETRY_MIN_BACKOFF=100ms
    - REDIS_RETRY_MAX_BACKOFF=3s
    - REDIS_DIAL_TIMEOUT=10s
    - REDIS_READ_TIMEOUT=5s
    - REDIS_WRITE_TIMEOUT=2s
    - REDIS_POOL_TIMEOUT=1s
    - GO_REDIS_WRITER_QUEUE_MAX=50000
    - GO_REDIS_ON_BACKPRESSURE=drop_telemetry_quarantine_critical
    - BINANCE_WS_TIMEFRAME=kline_1y
    - POLL_INTERVAL_HOURS=1
    - HTTP_PORT=2121
    - REDIS_CANDLES_HOST=redis-worker-1
    - REDIS_CANDLES_PORT=6379
    - REDIS_CANDLES_HOST_2=redis-worker-1
    - REDIS_CANDLES_PORT_2=6379
    - REDIS_TICKS_HOST=redis-ticks
    - REDIS_TICKS_PORT=6379
    - PROMETHEUS_PORT=2121
    - FUTURES_WS_READ_TIMEOUT=900s
    - FUTURES_WS_PING_PERIOD=20s
    - FUTURES_WS_WRITE_WAIT=10s
    - BINANCE_MAX_PAIRS_TO_FETCH=300
    - BINANCE_MIN_SYMBOLS_REQUIRED=3
    - BINANCE_MAX_PAIRS_TO_ADD=300
    - BINANCE_MIN_TRADES_24H=1000
    - USE_UNIFIED_PIPELINE=safe
    - REDIS_WORKER_HOST=redis-worker-1
    - REDIS_WORKER_PORT=6379
    - REDIS_WORKER_HOST_2=redis-worker-1
    - REDIS_WORKER_PORT_2=6379
    - REDIS_WORKER_HOST_3=redis-worker-3
    - REDIS_WORKER_PORT_3=6379
    - FUTURES_WS_ENABLED=false
    - HYPERLIQUID_WS_ENABLED=${HYPERLIQUID_WS_ENABLED:-false}
    - HYPERLIQUID_WS_READ_TIMEOUT=90s
    - HYPERLIQUID_WS_PING_PERIOD=20s
    - HYPERLIQUID_WS_WRITE_WAIT=5s
    - HYPERLIQUID_WS_MSG_CHAN_CAP=5000
    - ENABLE_DATA_COLLECTION=false
    - CROSSASSET_ENABLED=false
    - REST_CANDLE_TIMEOUT=60s
    - API_CLIENT_TIMEOUT=30s
    - WS_HANDSHAKE_TIMEOUT=45s
    - WS_DIAL_TIMEOUT=30s
    - WS_TCP_KEEPALIVE=60s
    - TZ=UTC
    depends_on:
      redis:
        condition: service_healthy
      redis-worker-1:
        condition: service_healthy
      redis-worker-2:
        condition: service_healthy
      redis-worker-3:
        condition: service_healthy
      redis-ticks:
        condition: service_healthy
    networks:
    - scanner-network
    ports:
    - 2121:2121
    restart: unless-stopped
    command:
    - sh
    - -c
    - sleep 210 && ./rest-fetcher
    deploy:
      resources:
        limits:
          memory: 400M
          cpus: '0.25'
        reservations:
          memory: 24M
    dns:
    - 8.8.8.8
    - 8.8.4.4
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    volumes:
    - ./logs:/app/logs
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:2121/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 900s
