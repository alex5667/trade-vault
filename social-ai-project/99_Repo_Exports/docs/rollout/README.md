# Rollout

Rollout/rollback playbooks (plan §16, §19). Skill: `social-rollout`. Ladder: local → replay/golden → shadow → draft/private → canary (platform/account) → ramp → enforce. Autopublish gated until SLOs stable; config-gated rollback (drop to draft-only / pause autopublish).
