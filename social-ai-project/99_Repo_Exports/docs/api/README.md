# API

Control-plane API (plan §12.2). Endpoints (Phase 6): GET /trends, GET /trends/:id, POST /briefs/generate, POST /scripts/generate, POST /assets/render, GET /review/queue, POST /review/:id/approve, POST /review/:id/reject, POST /publish/:id/schedule, POST /publish/:id/cancel, GET /outcomes, GET /experiments, POST /governors/:name/mode, POST /replay.
