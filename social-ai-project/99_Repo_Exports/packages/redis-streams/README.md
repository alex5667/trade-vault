# packages/redis-streams

Canonical Redis Streams names (plan §10) shared across TS (`streams.ts`) and Python (`streams.py`). Keep both in sync; renames require a contract check.

Also the home for producer/consumer-group helpers (Phase 3, Epic 3): producer helper, consumer-group helper, PEL recovery, DLQ writer, quarantine writer, replay runner.
