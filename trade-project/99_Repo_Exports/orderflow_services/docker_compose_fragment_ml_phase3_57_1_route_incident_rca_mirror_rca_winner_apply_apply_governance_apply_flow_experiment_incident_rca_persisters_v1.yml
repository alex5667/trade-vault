services:
  ml-route-incident-rca-apply-slo-rollups-persister-v3-57-1:
    build: .
    command: python -m orderflow_services.route_incident_rca_mirror_rca_winner_apply_apply_governance_apply_flow_experiment_incident_rca_apply_slo_rollups_persister_v3_57_1
    restart: unless-stopped
    environment:
      REDIS_URL: ${REDIS_URL}
      TIMESCALE_DSN: ${TIMESCALE_DSN}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ML_ROUTE_INCIDENT_RCA_APPLY_SLO_PERSISTER_PORT: ${ML_ROUTE_INCIDENT_RCA_APPLY_SLO_PERSISTER_PORT:-9995}
    ports:
      - "9995:9995"

  ml-route-incident-rca-apply-retry-results-persister-v3-57-1:
    build: .
    command: python -m orderflow_services.route_incident_rca_mirror_rca_winner_apply_apply_governance_apply_flow_experiment_incident_rca_apply_retry_results_persister_v3_57_1
    restart: unless-stopped
    environment:
      REDIS_URL: ${REDIS_URL}
      TIMESCALE_DSN: ${TIMESCALE_DSN}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ML_ROUTE_INCIDENT_RCA_APPLY_RETRY_PERSISTER_PORT: ${ML_ROUTE_INCIDENT_RCA_APPLY_RETRY_PERSISTER_PORT:-9996}
    ports:
      - "9996:9996"

  ml-route-incident-rca-apply-escalations-persister-v3-57-1:
    build: .
    command: python -m orderflow_services.route_incident_rca_mirror_rca_winner_apply_apply_governance_apply_flow_experiment_incident_rca_apply_escalations_persister_v3_57_1
    restart: unless-stopped
    environment:
      REDIS_URL: ${REDIS_URL}
      TIMESCALE_DSN: ${TIMESCALE_DSN}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ML_ROUTE_INCIDENT_RCA_APPLY_ESCALATIONS_PERSISTER_PORT: ${ML_ROUTE_INCIDENT_RCA_APPLY_ESCALATIONS_PERSISTER_PORT:-9997}
    ports:
      - "9997:9997"
