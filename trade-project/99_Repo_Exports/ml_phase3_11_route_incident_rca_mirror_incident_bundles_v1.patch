diff --git a/orderflow_services/route_incident_rca_mirror_incident_bundle_builder_v3_11.py b/orderflow_services/route_incident_rca_mirror_incident_bundle_builder_v3_11.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/orderflow_services/route_incident_rca_mirror_incident_bundle_builder_v3_11.py
@@ -0,0 +1,480 @@
+from __future__ import annotations
+
+import asyncio
+import json
+import os
+import time
+from typing import Any, Dict, List, Tuple
+
+try:  # pragma: no cover
+    import redis.asyncio as redis
+except Exception:  # pragma: no cover
+    redis = None
+
+try:  # pragma: no cover
+    import psycopg
+except Exception:  # pragma: no cover
+    psycopg = None
+
+try:  # pragma: no cover
+    from prometheus_client import Counter, Gauge, Histogram, start_http_server
+except Exception:  # pragma: no cover
+    Counter = Gauge = Histogram = None
+    def start_http_server(*args: Any, **kwargs: Any) -> None:
+        return None
+
+
+APP_NAME = "route_incident_rca_mirror_incident_bundle_builder_v3_11"
+ROLLOUT_JOURNAL_STREAM = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_ROLLOUT_JOURNAL_STREAM",
+    "stream:ml:route_incident_rca_mirror_rollout_journal",
+)
+VERIFICATION_STREAM = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_VERIFICATION_RESULTS_STREAM",
+    "stream:ml:route_incident_rca_mirror_verification_results",
+)
+RETRY_STREAM = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_RETRY_RESULTS_STREAM",
+    "stream:ml:route_incident_rca_mirror_retry_results",
+)
+ESCALATIONS_STREAM = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_ESCALATIONS_STREAM",
+    "stream:ml:route_incident_rca_mirror_escalations",
+)
+OUTPUT_STREAM = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_STREAM",
+    "stream:ml:route_incident_rca_mirror_incident_bundles",
+)
+AUDIT_STREAM = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_AUDIT_STREAM",
+    "stream:ml:route_incident_rca_mirror_incident_bundles_audit",
+)
+LAST_HASH = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_LAST_HASH",
+    "metrics:ml:route_incident_rca_mirror_incident_bundles:last",
+)
+GROUP = os.getenv("ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_GROUP", APP_NAME)
+CONSUMER = os.getenv("HOSTNAME", APP_NAME)
+PORT = int(os.getenv("ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_PORT", "9929"))
+MAXLEN = int(os.getenv("ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_MAXLEN", "20000"))
+LOOKBACK_COUNT = int(os.getenv("ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_LOOKBACK_COUNT", "50"))
+RECENT_WINDOW_MIN = int(os.getenv("ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_RECENT_WINDOW_MIN", "240"))
+ONLY_SEVERITY = os.getenv("ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_ONLY_SEVERITY", "warning,critical")
+TRIGGER_ON_TRANSITIONS = os.getenv(
+    "ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_TRIGGER_ON_TRANSITIONS",
+    "AUDIT_TO_MIRROR,MIRROR_TO_AUDIT",
+)
+
+
+def _counter(name: str, doc: str, labels: Tuple[str, ...] = ()) -> Any:
+    return Counter(name, doc, labels) if Counter else None
+
+
+def _gauge(name: str, doc: str, labels: Tuple[str, ...] = ()) -> Any:
+    return Gauge(name, doc, labels) if Gauge else None
+
+
+def _hist(name: str, doc: str, labels: Tuple[str, ...] = ()) -> Any:
+    return Histogram(name, doc, labels) if Histogram else None
+
+
+RUNS = _counter(
+    "ml_route_incident_rca_mirror_incident_bundles_runs_total",
+    "Mirror incident bundle builder runs",
+    ("status", "trigger_type"),
+)
+LAT = _hist(
+    "ml_route_incident_rca_mirror_incident_bundles_latency_seconds",
+    "Mirror incident bundle builder latency seconds",
+)
+UP = _gauge(
+    "ml_route_incident_rca_mirror_incident_bundles_up",
+    "Mirror incident bundle builder up",
+)
+LAST_RUN_TS = _gauge(
+    "ml_route_incident_rca_mirror_incident_bundles_last_run_ts_seconds",
+    "Mirror incident bundle builder last run timestamp",
+)
+BUNDLES = _counter(
+    "ml_route_incident_rca_mirror_incident_bundles_total",
+    "Mirror incident bundles created",
+    ("severity", "trigger_type"),
+)
+
+
+def now_ms() -> int:
+    return int(time.time() * 1000)
+
+
+def stable_json(obj: Any) -> str:
+    return json.dumps(obj, ensure_ascii=False, sort_keys=True, separators=(",", ":"))
+
+
+def parse_int(v: Any, default: int = 0) -> int:
+    try:
+        return int(float(v))
+    except Exception:
+        return default
+
+
+def as_dict(fields: Dict[Any, Any]) -> Dict[str, Any]:
+    out: Dict[str, Any] = {}
+    for k, v in fields.items():
+        kk = k.decode() if isinstance(k, (bytes, bytearray)) else str(k)
+        if isinstance(v, (bytes, bytearray)):
+            try:
+                out[kk] = v.decode()
+            except Exception:
+                out[kk] = v.hex()
+        else:
+            out[kk] = v
+    return out
+
+
+def maybe_json(v: Any, default: Any = None) -> Any:
+    if v is None:
+        return default
+    if isinstance(v, (dict, list)):
+        return v
+    try:
+        return json.loads(v)
+    except Exception:
+        return default
+
+
+def allowed_severities() -> set[str]:
+    return {x.strip().lower() for x in ONLY_SEVERITY.split(",") if x.strip()}
+
+
+def trigger_transitions() -> set[str]:
+    return {x.strip().upper() for x in TRIGGER_ON_TRANSITIONS.split(",") if x.strip()}
+
+
+async def ensure_group(client: Any, stream_key: str, group: str) -> None:
+    try:
+        await client.xgroup_create(stream_key, group, id="$", mkstream=True)
+    except Exception:
+        return
+
+
+async def xr_recent(client: Any, stream_key: str, count: int) -> List[Dict[str, Any]]:
+    try:
+        rows = await client.xrevrange(stream_key, count=count)
+    except Exception:
+        return []
+    out: List[Dict[str, Any]] = []
+    for entry_id, payload in rows:
+        row = as_dict(payload)
+        row["_stream_id"] = entry_id.decode() if isinstance(entry_id, (bytes, bytearray)) else str(entry_id)
+        out.append(row)
+    return out
+
+
+def within_recent_window(ts_ms: int) -> bool:
+    return ts_ms >= now_ms() - RECENT_WINDOW_MIN * 60 * 1000
+
+
+def normalize_trigger(source: str, row: Dict[str, Any]) -> Dict[str, Any]:
+    ts_ms = parse_int(row.get("ts_ms"), 0)
+    if source == "rollout_journal":
+        transition_type = str(row.get("transition_type") or "NONE").upper()
+        severity = "warning" if transition_type == "AUDIT_TO_MIRROR" else "critical"
+        return {
+            "trigger_type": "transition",
+            "transition_type": transition_type,
+            "severity": severity,
+            "reason_code": str(row.get("reason_code") or "UNKNOWN"),
+            "ts_ms": ts_ms,
+            "row": row,
+        }
+    summary = maybe_json(row.get("summary_json"), {})
+    severity = str(row.get("severity") or summary.get("severity") or "info").lower()
+    reason_codes = summary.get("reason_codes", []) if isinstance(summary, dict) else []
+    return {
+        "trigger_type": "escalation",
+        "transition_type": "NONE",
+        "severity": severity,
+        "reason_code": ",".join(reason_codes) if isinstance(reason_codes, list) else "UNKNOWN",
+        "ts_ms": ts_ms,
+        "row": row,
+    }
+
+
+def should_trigger_bundle(trigger: Dict[str, Any]) -> bool:
+    if not within_recent_window(trigger["ts_ms"]):
+        return False
+    if trigger["trigger_type"] == "transition":
+        return trigger["transition_type"] in trigger_transitions()
+    return trigger["severity"] in allowed_severities()
+
+
+def summarize_rows(rows: List[Dict[str, Any]], fields: List[str]) -> List[Dict[str, Any]]:
+    out: List[Dict[str, Any]] = []
+    for row in rows[:LOOKBACK_COUNT]:
+        item = {}
+        for f in fields:
+            if f in row:
+                item[f] = row[f]
+        out.append(item)
+    return out
+
+
+async def build_bundle(r: Any, trigger: Dict[str, Any]) -> Dict[str, Any]:
+    verification = await xr_recent(r, VERIFICATION_STREAM, LOOKBACK_COUNT)
+    retry = await xr_recent(r, RETRY_STREAM, LOOKBACK_COUNT)
+    escalations = await xr_recent(r, ESCALATIONS_STREAM, LOOKBACK_COUNT)
+    rollout_journal = await xr_recent(r, ROLLOUT_JOURNAL_STREAM, LOOKBACK_COUNT)
+
+    recent_verification = [r0 for r0 in verification if within_recent_window(parse_int(r0.get("ts_ms"), 0))]
+    recent_retry = [r0 for r0 in retry if within_recent_window(parse_int(r0.get("ts_ms"), 0))]
+    recent_escalations = [r0 for r0 in escalations if within_recent_window(parse_int(r0.get("ts_ms"), 0))]
+    recent_rollout = [r0 for r0 in rollout_journal if within_recent_window(parse_int(r0.get("ts_ms"), 0))]
+
+    verification_reason_codes = sorted({
+        str(r0.get("reason_code") or "")
+        for r0 in recent_verification
+        if str(r0.get("reason_code") or "")
+    })
+    retry_reason_codes = sorted({
+        str(r0.get("reason_code") or "")
+        for r0 in recent_retry
+        if str(r0.get("reason_code") or "")
+    })
+    escalation_severities = sorted({
+        str(r0.get("severity") or "")
+        for r0 in recent_escalations
+        if str(r0.get("severity") or "")
+    })
+
+    bundle_id = f"mirror-bundle:{trigger['trigger_type']}:{trigger['ts_ms']}"
+    return {
+        "schema_version": 1,
+        "bundle_id": bundle_id,
+        "contour": "route_incident_rca_mirror",
+        "trigger_type": trigger["trigger_type"],
+        "trigger_severity": trigger["severity"],
+        "trigger_reason_code": trigger["reason_code"],
+        "trigger_ts_ms": trigger["ts_ms"],
+        "summary": {
+            "verification_events_n": len(recent_verification),
+            "retry_events_n": len(recent_retry),
+            "escalation_events_n": len(recent_escalations),
+            "rollout_journal_events_n": len(recent_rollout),
+            "verification_reason_codes": verification_reason_codes,
+            "retry_reason_codes": retry_reason_codes,
+            "escalation_severities": escalation_severities,
+        },
+        "evidence": {
+            "trigger": trigger["row"],
+            "verification_recent": summarize_rows(
+                recent_verification,
+                ["decision", "reason_code", "current_mode", "target_mode", "ts_ms"],
+            ),
+            "retry_recent": summarize_rows(
+                recent_retry,
+                ["decision", "reason_code", "attempts", "target_mode", "ts_ms"],
+            ),
+            "escalations_recent": summarize_rows(
+                recent_escalations,
+                ["severity", "summary_json", "ts_ms"],
+            ),
+            "rollout_journal_recent": summarize_rows(
+                recent_rollout,
+                ["transition_type", "mode_before", "mode_after", "reason_code", "ts_ms"],
+            ),
+        },
+    }
+
+
+async def persist_if_configured(db_url: str, bundle: Dict[str, Any]) -> None:
+    if not db_url or psycopg is None:
+        return
+    with psycopg.connect(db_url) as conn:  # pragma: no cover
+        with conn.cursor() as cur:
+            cur.execute(
+                """
+                INSERT INTO llm_route_incident_rca_mirror_incident_bundles (
+                    bundle_id,
+                    ts_ms,
+                    contour,
+                    trigger_type,
+                    trigger_severity,
+                    trigger_reason_code,
+                    bundle_json
+                ) VALUES (
+                    %(bundle_id)s,
+                    %(ts_ms)s,
+                    %(contour)s,
+                    %(trigger_type)s,
+                    %(trigger_severity)s,
+                    %(trigger_reason_code)s,
+                    %(bundle_json)s
+                )
+                """,
+                {
+                    "bundle_id": bundle["bundle_id"],
+                    "ts_ms": now_ms(),
+                    "contour": bundle["contour"],
+                    "trigger_type": bundle["trigger_type"],
+                    "trigger_severity": bundle["trigger_severity"],
+                    "trigger_reason_code": bundle["trigger_reason_code"],
+                    "bundle_json": json.dumps(bundle),
+                },
+            )
+            conn.commit()
+
+
+async def process_trigger(r: Any, db_url: str, source: str, row: Dict[str, Any]) -> Tuple[str, str]:
+    trigger = normalize_trigger(source, row)
+    if not should_trigger_bundle(trigger):
+        return "SKIP", trigger["trigger_type"]
+
+    bundle = await build_bundle(r, trigger)
+    await persist_if_configured(db_url, bundle)
+    await r.xadd(
+        OUTPUT_STREAM,
+        {
+            "schema_version": 1,
+            "bundle_id": bundle["bundle_id"],
+            "contour": bundle["contour"],
+            "trigger_type": bundle["trigger_type"],
+            "trigger_severity": bundle["trigger_severity"],
+            "trigger_reason_code": bundle["trigger_reason_code"],
+            "bundle_json": stable_json(bundle),
+            "ts_ms": str(now_ms()),
+        },
+        maxlen=MAXLEN,
+        approximate=True,
+    )
+    await r.hset(
+        LAST_HASH,
+        mapping={
+            "bundle_id": bundle["bundle_id"],
+            "trigger_type": bundle["trigger_type"],
+            "trigger_severity": bundle["trigger_severity"],
+            "trigger_reason_code": bundle["trigger_reason_code"],
+            "ts_ms": str(now_ms()),
+        },
+    )
+    if BUNDLES:
+        BUNDLES.labels(severity=bundle["trigger_severity"], trigger_type=bundle["trigger_type"]).inc()
+    return "BUILT", bundle["trigger_type"]
+
+
+async def main() -> None:  # pragma: no cover
+    if redis is None:
+        raise RuntimeError("redis.asyncio is required")
+    start_http_server(PORT)
+    if UP:
+        UP.set(1)
+    r = redis.from_url(os.getenv("REDIS_URL", "redis://localhost:6379/0"))
+    db_url = os.getenv("DATABASE_URL", "")
+    await ensure_group(r, ROLLOUT_JOURNAL_STREAM, GROUP)
+    await ensure_group(r, ESCALATIONS_STREAM, GROUP)
+
+    while True:
+        rows = await r.xreadgroup(
+            GROUP,
+            CONSUMER,
+            {ROLLOUT_JOURNAL_STREAM: ">", ESCALATIONS_STREAM: ">"},
+            count=32,
+            block=5000,
+        )
+        if not rows:
+            continue
+        for stream_name, messages in rows:
+            source = "rollout_journal" if stream_name == ROLLOUT_JOURNAL_STREAM else "escalations"
+            for msg_id, payload in messages:
+                started = time.perf_counter()
+                status = "ok"
+                trigger_type = "none"
+                try:
+                    row = as_dict(payload)
+                    decision, trigger_type = await process_trigger(r, db_url, source, row)
+                    await r.xadd(
+                        AUDIT_STREAM,
+                        {
+                            "event_type": "ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLE_EVALUATED",
+                            "source": source,
+                            "decision": decision,
+                            "trigger_type": trigger_type,
+                            "ts_ms": str(now_ms()),
+                        },
+                        maxlen=MAXLEN,
+                        approximate=True,
+                    )
+                    await r.xack(stream_name, GROUP, msg_id)
+                    if LAST_RUN_TS:
+                        LAST_RUN_TS.set(time.time())
+                except Exception as exc:
+                    status = "error"
+                    await r.xadd(
+                        AUDIT_STREAM,
+                        {
+                            "event_type": "ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLE_FAILED",
+                            "source": source,
+                            "error": str(exc),
+                            "ts_ms": str(now_ms()),
+                        },
+                        maxlen=MAXLEN,
+                        approximate=True,
+                    )
+                    await r.xack(stream_name, GROUP, msg_id)
+                finally:
+                    if RUNS:
+                        RUNS.labels(status=status, trigger_type=trigger_type).inc()
+                    if LAT:
+                        LAT.observe(max(time.perf_counter() - started, 0.0))
+
+
+if __name__ == "__main__":  # pragma: no cover
+    asyncio.run(main())
diff --git a/orderflow_services/sql/ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.sql b/orderflow_services/sql/ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.sql
new file mode 100644
index 0000000..2222222
--- /dev/null
+++ b/orderflow_services/sql/ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.sql
@@ -0,0 +1,21 @@
+BEGIN;
+
+CREATE TABLE IF NOT EXISTS llm_route_incident_rca_mirror_incident_bundles (
+    id                  bigserial PRIMARY KEY,
+    bundle_id           text NOT NULL,
+    ts_ms               bigint NOT NULL,
+    contour             text NOT NULL,
+    trigger_type        text NOT NULL,
+    trigger_severity    text NOT NULL,
+    trigger_reason_code text NOT NULL,
+    bundle_json         jsonb NOT NULL
+);
+
+CREATE INDEX IF NOT EXISTS idx_llm_route_incident_rca_mirror_incident_bundles_ts
+    ON llm_route_incident_rca_mirror_incident_bundles(ts_ms DESC);
+
+CREATE INDEX IF NOT EXISTS idx_llm_route_incident_rca_mirror_incident_bundles_bundle_id
+    ON llm_route_incident_rca_mirror_incident_bundles(bundle_id);
+
+COMMIT;
diff --git a/orderflow_services/docker_compose_fragment_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.yml b/orderflow_services/docker_compose_fragment_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.yml
new file mode 100644
index 0000000..3333333
--- /dev/null
+++ b/orderflow_services/docker_compose_fragment_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.yml
@@ -0,0 +1,21 @@
+services:
+  scanner-route-incident-rca-mirror-incident-bundles-v3-11:
+    build: { context: ., dockerfile: python-worker/Dockerfile.gpu }
+    command: ["python", "-m", "orderflow_services.route_incident_rca_mirror_incident_bundle_builder_v3_11"]
+    restart: unless-stopped
+    environment:
+      REDIS_URL: ${REDIS_URL:-redis://redis-worker-1:6379/0}
+      DATABASE_URL: ${DATABASE_URL:-}
+      ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_PORT: ${ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_PORT:-9929}
+      ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_LOOKBACK_COUNT: ${ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_LOOKBACK_COUNT:-50}
+      ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_RECENT_WINDOW_MIN: ${ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_RECENT_WINDOW_MIN:-240}
+      ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_ONLY_SEVERITY: ${ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_ONLY_SEVERITY:-warning,critical}
+      ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_TRIGGER_ON_TRANSITIONS: ${ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_TRIGGER_ON_TRANSITIONS:-AUDIT_TO_MIRROR,MIRROR_TO_AUDIT}
+    ports: ["${ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_PORT:-9929}:9929"]
+    depends_on: [redis-worker-1]
diff --git a/orderflow_services/prometheus_alerts_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.yml b/orderflow_services/prometheus_alerts_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.yml
new file mode 100644
index 0000000..4444444
--- /dev/null
+++ b/orderflow_services/prometheus_alerts_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.yml
@@ -0,0 +1,31 @@
+groups:
+  - name: ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1
+    rules:
+      - alert: MLRouteIncidentRCAMirrorIncidentBundlesStale
+        expr: time() - ml_route_incident_rca_mirror_incident_bundles_last_run_ts_seconds > 1800
+        for: 10m
+        labels: { severity: warning }
+        annotations:
+          summary: "route incident RCA mirror incident bundle builder is stale"
+
+      - alert: MLRouteIncidentRCAMirrorIncidentBundlesErrors
+        expr: increase(ml_route_incident_rca_mirror_incident_bundles_runs_total{status="error"}[30m]) > 0
+        for: 5m
+        labels: { severity: warning }
+        annotations:
+          summary: "route incident RCA mirror incident bundle builder has errors"
+
+      - alert: MLRouteIncidentRCAMirrorIncidentBundlesBuilt
+        expr: increase(ml_route_incident_rca_mirror_incident_bundles_total{severity="critical"}[30m]) > 0
+        for: 1m
+        labels: { severity: info }
+        annotations:
+          summary: "route incident RCA mirror critical incident bundle was built"
diff --git a/orderflow_services/runbook_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.md b/orderflow_services/runbook_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.md
new file mode 100644
index 0000000..5555555
--- /dev/null
+++ b/orderflow_services/runbook_ml_phase3_11_route_incident_rca_mirror_incident_bundles_v1.md
@@ -0,0 +1,70 @@
+# Phase 3.11 — Route Incident RCA Mirror Incident Bundle Builder
+
+## Цель
+Собрать единый forensic bundle для нового `route_incident_rca mirror-governance contour`,
+чтобы его можно было использовать для:
+- RCA
+- local / Vertex summarization
+- отчётов
+- пост-анализа rollout / rollback инцидентов
+
+## Что делает
+- слушает triggers из:
+  - `stream:ml:route_incident_rca_mirror_rollout_journal`
+  - `stream:ml:route_incident_rca_mirror_escalations`
+- для trigger собирает недавний контекст из:
+  - `stream:ml:route_incident_rca_mirror_verification_results`
+  - `stream:ml:route_incident_rca_mirror_retry_results`
+  - `stream:ml:route_incident_rca_mirror_escalations`
+  - `stream:ml:route_incident_rca_mirror_rollout_journal`
+- строит единый bundle JSON
+- пишет:
+  - `stream:ml:route_incident_rca_mirror_incident_bundles`
+  - `metrics:ml:route_incident_rca_mirror_incident_bundles:last`
+
+## Triggers
+- transition:
+  - `AUDIT_TO_MIRROR`
+  - `MIRROR_TO_AUDIT`
+- escalation severity:
+  - `warning`
+  - `critical`
+
+## Safe start
+```bash
+export ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_LOOKBACK_COUNT=50
+export ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_RECENT_WINDOW_MIN=240
+export ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_ONLY_SEVERITY=warning,critical
+export ML_ROUTE_INCIDENT_RCA_MIRROR_INCIDENT_BUNDLES_TRIGGER_ON_TRANSITIONS=AUDIT_TO_MIRROR,MIRROR_TO_AUDIT
+```
+
+## Smoke checks
+```bash
+curl -s localhost:9929/metrics | grep '^ml_route_incident_rca_mirror_incident_bundles_'
+redis-cli HGETALL metrics:ml:route_incident_rca_mirror_incident_bundles:last
+redis-cli XREVRANGE stream:ml:route_incident_rca_mirror_incident_bundles + - COUNT 5
+redis-cli XREVRANGE stream:ml:route_incident_rca_mirror_incident_bundles_audit + - COUNT 5
+```
+
+## Notes
+- scope только `scanner_infra`
+- hot path не затронут
+- bundle builder advisory-only
+- это единый forensic ingress для следующего RCA/reporting слоя
diff --git a/orderflow_services/tests/test_route_incident_rca_mirror_incident_bundles_v3_11.py b/orderflow_services/tests/test_route_incident_rca_mirror_incident_bundles_v3_11.py
new file mode 100644
index 0000000..6666666
--- /dev/null
+++ b/orderflow_services/tests/test_route_incident_rca_mirror_incident_bundles_v3_11.py
@@ -0,0 +1,55 @@
+from orderflow_services.route_incident_rca_mirror_incident_bundle_builder_v3_11 import (
+    normalize_trigger,
+    should_trigger_bundle,
+)
+
+
+def test_normalize_trigger_for_transition():
+    row = {
+        "transition_type": "MIRROR_TO_AUDIT",
+        "reason_code": "MISMATCH_RATE_TOO_HIGH",
+        "ts_ms": "9999999999999",
+    }
+    out = normalize_trigger("rollout_journal", row)
+    assert out["trigger_type"] == "transition"
+    assert out["severity"] == "critical"
+
+
+def test_normalize_trigger_for_escalation():
+    row = {
+        "severity": "warning",
+        "summary_json": '{"severity":"warning","reason_codes":["ROLLBACK_MTTR_P95_HIGH"]}',
+        "ts_ms": "9999999999999",
+    }
+    out = normalize_trigger("escalations", row)
+    assert out["trigger_type"] == "escalation"
+    assert out["severity"] == "warning"
+
+
+def test_should_trigger_bundle_for_critical_escalation():
+    trigger = {
+        "trigger_type": "escalation",
+        "transition_type": "NONE",
+        "severity": "critical",
+        "ts_ms": 9999999999999,
+        "row": {},
+    }
+    assert should_trigger_bundle(trigger) is True
+