diff --git a/orderflow_services/emergency_summarize_handoff_rewire_adapter_v3_4.py b/orderflow_services/emergency_summarize_handoff_rewire_adapter_v3_4.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/orderflow_services/emergency_summarize_handoff_rewire_adapter_v3_4.py
@@ -0,0 +1,454 @@
+from __future__ import annotations
+
+import json
+import os
+import time
+from typing import Any, Dict, Tuple
+
+try:  # pragma: no cover
+    import redis.asyncio as redis
+except Exception:  # pragma: no cover
+    redis = None
+
+try:  # pragma: no cover
+    import psycopg
+except Exception:  # pragma: no cover
+    psycopg = None
+
+try:  # pragma: no cover
+    from prometheus_client import Counter, Gauge, Histogram, start_http_server
+except Exception:  # pragma: no cover
+    Counter = Gauge = Histogram = None
+    def start_http_server(*args: Any, **kwargs: Any) -> None:
+        return None
+
+
+APP_NAME = "emergency_summarize_handoff_rewire_adapter_v3_4"
+INPUT_STREAM = os.getenv(
+    "ML_EMERGENCY_SUMMARIZE_HANDOFF_SOURCE_STREAM",
+    "stream:ml:emergency_summarize_handoff_source",
+)
+OUTPUT_STREAM = os.getenv(
+    "ML_VERTEX_LOCAL_HANDOFF_INPUT_STREAM",
+    "stream:ml:vertex_local_handoff_requests",
+)
+DECISIONS_STREAM = os.getenv(
+    "ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_DECISIONS_STREAM",
+    "stream:ml:emergency_summarize_handoff_rewire_decisions",
+)
+AUDIT_STREAM = os.getenv(
+    "ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_AUDIT_STREAM",
+    "stream:ml:emergency_summarize_handoff_rewire_audit",
+)
+LAST_HASH = os.getenv(
+    "ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_LAST_HASH",
+    "metrics:ml:emergency_summarize_handoff_rewire:last",
+)
+GLOBAL_POLICY_KEY = os.getenv(
+    "ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_GLOBAL_POLICY_KEY",
+    "cfg:ml:emergency_summarize_handoff_rewire:global",
+)
+GROUP = os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_GROUP", APP_NAME)
+CONSUMER = os.getenv("HOSTNAME", APP_NAME)
+PORT = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_PORT", "9920"))
+MAXLEN = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MAXLEN", "20000"))
+
+DEFAULT_ENABLED = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ENABLED", "1"))
+DEFAULT_MODE = os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MODE", "ENABLED").upper()
+DEFAULT_MAX_PAYLOAD_BYTES = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MAX_PAYLOAD_BYTES", "65536"))
+DEFAULT_MAX_PROMPT_CHARS = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MAX_PROMPT_CHARS", "8000"))
+DEFAULT_FORCE_LOCAL = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_FORCE_LOCAL", "0"))
+DEFAULT_ALLOW_WARNING = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_WARNING", "1"))
+DEFAULT_ALLOW_CRITICAL = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_CRITICAL", "1"))
+DEFAULT_ALLOW_INFO = int(os.getenv("ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_INFO", "0"))
+
+
+def _counter(name: str, doc: str, labels: Tuple[str, ...] = ()) -> Any:
+    return Counter(name, doc, labels) if Counter else None
+
+
+def _gauge(name: str, doc: str, labels: Tuple[str, ...] = ()) -> Any:
+    return Gauge(name, doc, labels) if Gauge else None
+
+
+def _hist(name: str, doc: str, labels: Tuple[str, ...] = ()) -> Any:
+    return Histogram(name, doc, labels) if Histogram else None
+
+
+RUNS = _counter(
+    "ml_emergency_summarize_handoff_rewire_runs_total",
+    "Emergency summarize handoff rewire runs",
+    ("status", "decision"),
+)
+LAT = _hist(
+    "ml_emergency_summarize_handoff_rewire_latency_seconds",
+    "Emergency summarize handoff rewire latency seconds",
+)
+UP = _gauge(
+    "ml_emergency_summarize_handoff_rewire_up",
+    "Emergency summarize handoff rewire adapter up",
+)
+LAST_RUN_TS = _gauge(
+    "ml_emergency_summarize_handoff_rewire_last_run_ts_seconds",
+    "Emergency summarize handoff rewire adapter last run timestamp",
+)
+ROUTED = _counter(
+    "ml_emergency_summarize_handoff_rewire_routed_total",
+    "Emergency summarize handoff rewire routed rows",
+    ("decision",),
+)
+
+
+def now_ms() -> int:
+    return int(time.time() * 1000)
+
+
+def parse_int(value: Any, default: int = 0) -> int:
+    try:
+        return int(float(value))
+    except Exception:
+        return default
+
+
+def stable_json(obj: Any) -> str:
+    return json.dumps(obj, ensure_ascii=False, sort_keys=True, separators=(",", ":"))
+
+
+def as_dict(fields: Dict[Any, Any]) -> Dict[str, Any]:
+    out: Dict[str, Any] = {}
+    for k, v in fields.items():
+        kk = k.decode() if isinstance(k, (bytes, bytearray)) else str(k)
+        if isinstance(v, (bytes, bytearray)):
+            try:
+                out[kk] = v.decode()
+            except Exception:
+                out[kk] = v.hex()
+        else:
+            out[kk] = v
+    return out
+
+
+def maybe_json(value: Any, default: Any = None) -> Any:
+    if value is None:
+        return default
+    if isinstance(value, (dict, list)):
+        return value
+    try:
+        return json.loads(value)
+    except Exception:
+        return default
+
+
+def policy_from_hash(raw: Dict[str, Any]) -> Dict[str, Any]:
+    return {
+        "enabled": parse_int(raw.get("enabled"), DEFAULT_ENABLED),
+        "mode": str(raw.get("mode") or DEFAULT_MODE).upper(),
+        "max_payload_bytes": parse_int(raw.get("max_payload_bytes"), DEFAULT_MAX_PAYLOAD_BYTES),
+        "max_prompt_chars": parse_int(raw.get("max_prompt_chars"), DEFAULT_MAX_PROMPT_CHARS),
+        "force_local": parse_int(raw.get("force_local"), DEFAULT_FORCE_LOCAL),
+        "allow_warning": parse_int(raw.get("allow_warning"), DEFAULT_ALLOW_WARNING),
+        "allow_critical": parse_int(raw.get("allow_critical"), DEFAULT_ALLOW_CRITICAL),
+        "allow_info": parse_int(raw.get("allow_info"), DEFAULT_ALLOW_INFO),
+    }
+
+
+def severity_allowed(severity: str, policy: Dict[str, Any]) -> bool:
+    sev = severity.lower()
+    if sev == "critical":
+        return policy["allow_critical"] == 1
+    if sev == "warning":
+        return policy["allow_warning"] == 1
+    if sev == "info":
+        return policy["allow_info"] == 1
+    return False
+
+
+def build_payload_json(row: Dict[str, Any]) -> str:
+    payload_json = row.get("payload_json")
+    if payload_json:
+        return str(payload_json)
+    prompt = str(row.get("prompt") or "")
+    title = str(row.get("title") or "")
+    context = maybe_json(row.get("context_json"), {})
+    summary = str(row.get("summary") or "")
+    if prompt or title or context or summary:
+        return stable_json({
+            "title": title,
+            "prompt": prompt,
+            "summary": summary,
+            "context": context,
+        })
+    return stable_json(row)
+
+
+def evaluate_row(row: Dict[str, Any], policy: Dict[str, Any]) -> Dict[str, Any]:
+    request_id = str(row.get("request_id") or "")
+    prompt = str(row.get("prompt") or "")
+    severity = str(row.get("severity") or "info")
+    payload_json = build_payload_json(row)
+
+    out = {
+        "decision": "REJECT",
+        "reason_code": "REJECTED",
+        "request_id": request_id,
+    }
+    if policy["enabled"] != 1:
+        out["reason_code"] = "DISABLED"
+        return out
+    if policy["mode"] == "DISABLED":
+        out["reason_code"] = "MODE_DISABLED"
+        return out
+    if not request_id:
+        out["reason_code"] = "REQUEST_ID_MISSING"
+        return out
+    if not severity_allowed(severity, policy):
+        out["reason_code"] = "SEVERITY_NOT_ALLOWED"
+        return out
+    if len(prompt) > policy["max_prompt_chars"]:
+        out["reason_code"] = "PROMPT_TOO_LARGE"
+        return out
+    if len(payload_json.encode("utf-8")) > policy["max_payload_bytes"]:
+        out["reason_code"] = "PAYLOAD_TOO_LARGE"
+        return out
+    out["decision"] = "ROUTE_HANDOFF"
+    out["reason_code"] = "OK"
+    return out
+
+
+def build_handoff_row(row: Dict[str, Any], policy: Dict[str, Any]) -> Dict[str, Any]:
+    severity = str(row.get("severity") or "warning")
+    return {
+        "schema_version": 1,
+        "request_id": str(row.get("request_id") or ""),
+        "task_family": "emergency_summarize",
+        "incident_id": str(row.get("incident_id") or ""),
+        "task_type": "emergency_summarize",
+        "severity": severity,
+        "source": "emergency_summarize_handoff_rewire_v3_4",
+        "payload_json": build_payload_json(row),
+        "compact_hash": str(row.get("compact_hash") or ""),
+        "vertex_unavailable": str(parse_int(row.get("vertex_unavailable"), 0)),
+        "force_local": str(max(parse_int(row.get("force_local"), 0), policy["force_local"])),
+        "ts_ms": str(now_ms()),
+    }
+
+
+async def ensure_group(client: Any, stream_key: str, group: str) -> None:
+    try:
+        await client.xgroup_create(stream_key, group, id="$", mkstream=True)
+    except Exception:
+        return
+
+
+async def read_policy(r: Any) -> Dict[str, Any]:
+    raw = await r.hgetall(GLOBAL_POLICY_KEY)
+    return policy_from_hash(as_dict(raw))
+
+
+async def persist_if_configured(db_url: str, row: Dict[str, Any], decision: Dict[str, Any], handoff_row: Dict[str, Any] | None) -> None:
+    if not db_url or psycopg is None:
+        return
+    with psycopg.connect(db_url) as conn:  # pragma: no cover
+        with conn.cursor() as cur:
+            cur.execute(
+                """
+                INSERT INTO llm_emergency_summarize_handoff_rewire_decisions (
+                    request_id,
+                    ts_ms,
+                    decision,
+                    reason_code,
+                    source_stream,
+                    output_stream,
+                    handoff_payload_json,
+                    original_payload_json
+                ) VALUES (
+                    %(request_id)s,
+                    %(ts_ms)s,
+                    %(decision)s,
+                    %(reason_code)s,
+                    %(source_stream)s,
+                    %(output_stream)s,
+                    %(handoff_payload_json)s,
+                    %(original_payload_json)s
+                )
+                """,
+                {
+                    "request_id": row.get("request_id", ""),
+                    "ts_ms": now_ms(),
+                    "decision": decision["decision"],
+                    "reason_code": decision["reason_code"],
+                    "source_stream": INPUT_STREAM,
+                    "output_stream": OUTPUT_STREAM if handoff_row else "",
+                    "handoff_payload_json": json.dumps(handoff_row or {}),
+                    "original_payload_json": json.dumps(row),
+                },
+            )
+            conn.commit()
+
+
+async def main() -> None:  # pragma: no cover
+    if redis is None:
+        raise RuntimeError("redis.asyncio is required")
+    start_http_server(PORT)
+    if UP:
+        UP.set(1)
+    r = redis.from_url(os.getenv("REDIS_URL", "redis://localhost:6379/0"))
+    await ensure_group(r, INPUT_STREAM, GROUP)
+    db_url = os.getenv("DATABASE_URL", "")
+
+    while True:
+        rows = await r.xreadgroup(GROUP, CONSUMER, {INPUT_STREAM: ">"}, count=32, block=5000)
+        if not rows:
+            continue
+        for _stream, messages in rows:
+            for msg_id, payload in messages:
+                started = time.perf_counter()
+                status = "ok"
+                decision_label = "REJECT"
+                try:
+                    row = as_dict(payload)
+                    policy = await read_policy(r)
+                    decision = evaluate_row(row, policy)
+                    decision_label = decision["decision"]
+                    handoff_row = None
+
+                    if decision["decision"] == "ROUTE_HANDOFF":
+                        handoff_row = build_handoff_row(row, policy)
+                        await r.xadd(OUTPUT_STREAM, handoff_row, maxlen=MAXLEN, approximate=True)
+
+                    await persist_if_configured(db_url, row, decision, handoff_row)
+
+                    decision_payload = {
+                        "schema_version": 1,
+                        "request_id": str(row.get("request_id") or ""),
+                        "decision": decision["decision"],
+                        "reason_code": decision["reason_code"],
+                        "source_stream": INPUT_STREAM,
+                        "output_stream": OUTPUT_STREAM if handoff_row else "",
+                        "ts_ms": str(now_ms()),
+                    }
+                    await r.xadd(DECISIONS_STREAM, decision_payload, maxlen=MAXLEN, approximate=True)
+                    await r.xadd(
+                        AUDIT_STREAM,
+                        {
+                            "event_type": "EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_DECIDED",
+                            **decision_payload,
+                        },
+                        maxlen=MAXLEN,
+                        approximate=True,
+                    )
+                    await r.hset(
+                        LAST_HASH,
+                        mapping={
+                            "request_id": str(row.get("request_id") or ""),
+                            "decision": decision["decision"],
+                            "reason_code": decision["reason_code"],
+                            "source_stream": INPUT_STREAM,
+                            "output_stream": OUTPUT_STREAM if handoff_row else "",
+                            "ts_ms": str(now_ms()),
+                        },
+                    )
+                    if ROUTED:
+                        ROUTED.labels(decision=decision["decision"]).inc()
+                    await r.xack(INPUT_STREAM, GROUP, msg_id)
+                    if LAST_RUN_TS:
+                        LAST_RUN_TS.set(time.time())
+                except Exception as exc:
+                    status = "error"
+                    await r.xadd(
+                        AUDIT_STREAM,
+                        {
+                            "event_type": "EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_FAILED",
+                            "error": str(exc),
+                            "ts_ms": str(now_ms()),
+                        },
+                        maxlen=MAXLEN,
+                        approximate=True,
+                    )
+                    await r.xack(INPUT_STREAM, GROUP, msg_id)
+                finally:
+                    if RUNS:
+                        RUNS.labels(status=status, decision=decision_label).inc()
+                    if LAT:
+                        LAT.observe(max(time.perf_counter() - started, 0.0))
+
+
+if __name__ == "__main__":  # pragma: no cover
+    import asyncio
+    asyncio.run(main())
diff --git a/orderflow_services/sql/ml_phase3_4_emergency_summarize_handoff_rewire_v1.sql b/orderflow_services/sql/ml_phase3_4_emergency_summarize_handoff_rewire_v1.sql
new file mode 100644
index 0000000..2222222
--- /dev/null
+++ b/orderflow_services/sql/ml_phase3_4_emergency_summarize_handoff_rewire_v1.sql
@@ -0,0 +1,21 @@
+BEGIN;
+
+CREATE TABLE IF NOT EXISTS llm_emergency_summarize_handoff_rewire_decisions (
+    id                    bigserial PRIMARY KEY,
+    request_id            text NOT NULL,
+    ts_ms                 bigint NOT NULL,
+    decision              text NOT NULL,
+    reason_code           text NOT NULL,
+    source_stream         text NOT NULL,
+    output_stream         text NOT NULL,
+    handoff_payload_json  jsonb NOT NULL,
+    original_payload_json jsonb NOT NULL
+);
+
+CREATE INDEX IF NOT EXISTS idx_llm_emergency_summarize_handoff_rewire_decisions_ts
+    ON llm_emergency_summarize_handoff_rewire_decisions(ts_ms DESC);
+
+COMMIT;
diff --git a/orderflow_services/docker_compose_fragment_ml_phase3_4_emergency_summarize_handoff_rewire_v1.yml b/orderflow_services/docker_compose_fragment_ml_phase3_4_emergency_summarize_handoff_rewire_v1.yml
new file mode 100644
index 0000000..3333333
--- /dev/null
+++ b/orderflow_services/docker_compose_fragment_ml_phase3_4_emergency_summarize_handoff_rewire_v1.yml
@@ -0,0 +1,24 @@
+services:
+  scanner-emergency-summarize-handoff-rewire-v3-4:
+    build: { context: ., dockerfile: python-worker/Dockerfile.gpu }
+    command: ["python", "-m", "orderflow_services.emergency_summarize_handoff_rewire_adapter_v3_4"]
+    restart: unless-stopped
+    environment:
+      REDIS_URL: ${REDIS_URL:-redis://redis-worker-1:6379/0}
+      DATABASE_URL: ${DATABASE_URL:-}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_PORT: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_PORT:-9920}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_SOURCE_STREAM: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_SOURCE_STREAM:-stream:ml:emergency_summarize_handoff_source}
+      ML_VERTEX_LOCAL_HANDOFF_INPUT_STREAM: ${ML_VERTEX_LOCAL_HANDOFF_INPUT_STREAM:-stream:ml:vertex_local_handoff_requests}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ENABLED: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ENABLED:-1}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MODE: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MODE:-ENABLED}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MAX_PAYLOAD_BYTES: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MAX_PAYLOAD_BYTES:-65536}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MAX_PROMPT_CHARS: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MAX_PROMPT_CHARS:-8000}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_FORCE_LOCAL: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_FORCE_LOCAL:-0}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_WARNING: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_WARNING:-1}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_CRITICAL: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_CRITICAL:-1}
+      ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_INFO: ${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_INFO:-0}
+    ports: ["${ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_PORT:-9920}:9920"]
+    depends_on: [redis-worker-1]
diff --git a/orderflow_services/prometheus_alerts_ml_phase3_4_emergency_summarize_handoff_rewire_v1.yml b/orderflow_services/prometheus_alerts_ml_phase3_4_emergency_summarize_handoff_rewire_v1.yml
new file mode 100644
index 0000000..4444444
--- /dev/null
+++ b/orderflow_services/prometheus_alerts_ml_phase3_4_emergency_summarize_handoff_rewire_v1.yml
@@ -0,0 +1,31 @@
+groups:
+  - name: ml_phase3_4_emergency_summarize_handoff_rewire_v1
+    rules:
+      - alert: MLEmergencySummarizeHandoffRewireStale
+        expr: time() - ml_emergency_summarize_handoff_rewire_last_run_ts_seconds > 1800
+        for: 10m
+        labels: { severity: warning }
+        annotations:
+          summary: "emergency summarize handoff rewire adapter is stale"
+
+      - alert: MLEmergencySummarizeHandoffRewireErrors
+        expr: increase(ml_emergency_summarize_handoff_rewire_runs_total{status="error"}[30m]) > 0
+        for: 5m
+        labels: { severity: warning }
+        annotations:
+          summary: "emergency summarize handoff rewire adapter has errors"
+
+      - alert: MLEmergencySummarizeHandoffRewireRejects
+        expr: increase(ml_emergency_summarize_handoff_rewire_routed_total{decision="REJECT"}[30m]) > 10
+        for: 10m
+        labels: { severity: warning }
+        annotations:
+          summary: "emergency summarize handoff rewire is rejecting requests"
diff --git a/orderflow_services/runbook_ml_phase3_4_emergency_summarize_handoff_rewire_v1.md b/orderflow_services/runbook_ml_phase3_4_emergency_summarize_handoff_rewire_v1.md
new file mode 100644
index 0000000..5555555
--- /dev/null
+++ b/orderflow_services/runbook_ml_phase3_4_emergency_summarize_handoff_rewire_v1.md
@@ -0,0 +1,66 @@
+# Phase 3.4 — Emergency Summarize Producer → Handoff Ingress Rewire
+
+## Цель
+Сделать следующий safest contour для bounded summary path:
+- `emergency_summarize`
+
+## Что делает
+- читает из промежуточного producer stream:
+  - `stream:ml:emergency_summarize_handoff_source`
+- валидирует bounded contract
+- шлёт в handoff ingress:
+  - `stream:ml:vertex_local_handoff_requests`
+- маркирует:
+  - `task_family=emergency_summarize`
+
+## Почему это следующий safest contour
+- bounded summary payload
+- не primary RCA
+- удобен для аварийных report/summarize сценариев
+- severity можно жёстко ограничить policy
+
+## Важный rollout принцип
+Этот шаг **не требует немедленно править producer код**.
+Сначала:
+1. переведите выбранный producer на output stream `stream:ml:emergency_summarize_handoff_source`
+2. включите adapter
+3. проверьте, что handoff decisions и routing корректны
+
+## Streams
+- input: `stream:ml:emergency_summarize_handoff_source`
+- output: `stream:ml:vertex_local_handoff_requests`
+- decisions: `stream:ml:emergency_summarize_handoff_rewire_decisions`
+- audit: `stream:ml:emergency_summarize_handoff_rewire_audit`
+
+## Safe start
+```bash
+export ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ENABLED=1
+export ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_MODE=ENABLED
+export ML_EMERGENCY_SUMMARIZE_HANDOFF_SOURCE_STREAM=stream:ml:emergency_summarize_handoff_source
+export ML_VERTEX_LOCAL_HANDOFF_INPUT_STREAM=stream:ml:vertex_local_handoff_requests
+export ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_WARNING=1
+export ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_CRITICAL=1
+export ML_EMERGENCY_SUMMARIZE_HANDOFF_REWIRE_ALLOW_INFO=0
+```
+
+## Smoke checks
+```bash
+redis-cli XADD stream:ml:emergency_summarize_handoff_source * \
+  request_id es-1 \
+  severity critical \
+  title "Emergency summary" \
+  prompt "Summarize the current service degradation in bounded form." \
+  ts_ms $(date +%s%3N)
+
+redis-cli XREVRANGE stream:ml:emergency_summarize_handoff_rewire_decisions + - COUNT 5
+redis-cli XREVRANGE stream:ml:vertex_local_handoff_requests + - COUNT 5
+redis-cli XREVRANGE stream:ml:emergency_summarize_handoff_rewire_audit + - COUNT 5
+curl -s localhost:9920/metrics | grep '^ml_emergency_summarize_handoff_rewire_'
+```
+
+## Notes
+- scope только `scanner_infra`
+- hot path не затронут
+- adapter reversible
+- severity gating обязательна
diff --git a/orderflow_services/tests/test_emergency_summarize_handoff_rewire_v3_4.py b/orderflow_services/tests/test_emergency_summarize_handoff_rewire_v3_4.py
new file mode 100644
index 0000000..6666666
--- /dev/null
+++ b/orderflow_services/tests/test_emergency_summarize_handoff_rewire_v3_4.py
@@ -0,0 +1,53 @@
+from orderflow_services.emergency_summarize_handoff_rewire_adapter_v3_4 import (
+    build_handoff_row,
+    evaluate_row,
+)
+
+
+def _policy():
+    return {
+        "enabled": 1,
+        "mode": "ENABLED",
+        "max_payload_bytes": 65536,
+        "max_prompt_chars": 8000,
+        "force_local": 0,
+        "allow_warning": 1,
+        "allow_critical": 1,
+        "allow_info": 0,
+    }
+
+
+def test_evaluate_row_rejects_missing_request_id():
+    row = {"prompt": "hello", "severity": "critical"}
+    out = evaluate_row(row, _policy())
+    assert out["decision"] == "REJECT"
+    assert out["reason_code"] == "REQUEST_ID_MISSING"
+
+
+def test_evaluate_row_rejects_info_severity_by_default():
+    row = {"request_id": "es-1", "prompt": "hello", "severity": "info"}
+    out = evaluate_row(row, _policy())
+    assert out["decision"] == "REJECT"
+    assert out["reason_code"] == "SEVERITY_NOT_ALLOWED"
+
+
+def test_build_handoff_row_sets_emergency_summarize_family():
+    row = {
+        "request_id": "es-1",
+        "severity": "critical",
+        "title": "Emergency summary",
+        "prompt": "Summarize degradation",
+    }
+    out = build_handoff_row(row, _policy())
+    assert out["task_family"] == "emergency_summarize"
+    assert out["task_type"] == "emergency_summarize"
+    assert out["request_id"] == "es-1"
