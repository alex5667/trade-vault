diff -ruN '--exclude=.pytest_cache' p5tree/binance_execution/test_execution_consistency_checker_v1.py p6tree/binance_execution/test_execution_consistency_checker_v1.py
--- p5tree/binance_execution/test_execution_consistency_checker_v1.py	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/binance_execution/test_execution_consistency_checker_v1.py	2026-03-06 20:13:39.999076946 +0000
@@ -0,0 +1,36 @@
+from __future__ import annotations
+
+import importlib.util
+import sys
+from pathlib import Path
+
+SCRIPT = Path(__file__).resolve().parent.parent / 'scripts' / 'check_execution_consistency.py'
+SPEC = importlib.util.spec_from_file_location('check_execution_consistency', SCRIPT)
+mod = importlib.util.module_from_spec(SPEC)
+assert SPEC and SPEC.loader
+sys.modules[SPEC.name] = mod
+SPEC.loader.exec_module(mod)
+
+
+def test_compare_execution_views_detects_sql_missing_and_state_mismatch():
+    redis_state = {
+        'sid-1': {'sid': 'sid-1', 'symbol': 'BTCUSDT', 'status': 'open', 'fsm_state': 'PROTECTED', 'sl_algo_id': 11}
+    }
+    stream_latest = {
+        'sid-1': {'sid': 'sid-1', 'symbol': 'BTCUSDT', 'status': 'open', 'fsm_state': 'PROTECTED'}
+    }
+    sql_orders = {}
+    mismatches = mod.compare_execution_views(redis_state, stream_latest, sql_orders, {})
+    categories = {m.category for m in mismatches}
+    assert 'sql_missing' in categories
+
+
+def test_summarise_mismatches_counts_severity():
+    mismatches = [
+        mod.ConsistencyMismatch('a', 'critical', 'sql_missing', 'x'),
+        mod.ConsistencyMismatch('b', 'warning', 'stream_missing', 'y'),
+    ]
+    summary = mod.summarise_mismatches(1, 1, 0, mismatches)
+    assert summary.critical_mismatches == 1
+    assert summary.warning_mismatches == 1
+    assert summary.mismatches_total == 2
diff -ruN '--exclude=.pytest_cache' p5tree/binance_execution/test_runbook_server_v1.py p6tree/binance_execution/test_runbook_server_v1.py
--- p5tree/binance_execution/test_runbook_server_v1.py	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/binance_execution/test_runbook_server_v1.py	2026-03-06 20:14:35.300656664 +0000
@@ -0,0 +1,25 @@
+from __future__ import annotations
+
+import importlib.util
+import json
+import sys
+from pathlib import Path
+
+SCRIPT = Path(__file__).resolve().parent.parent / 'runbooks' / 'server' / 'runbook_server.py'
+SPEC = importlib.util.spec_from_file_location('runbook_server', SCRIPT)
+mod = importlib.util.module_from_spec(SPEC)
+assert SPEC and SPEC.loader
+sys.modules[SPEC.name] = mod
+SPEC.loader.exec_module(mod)
+
+
+def test_render_index_shows_runbooks_and_latest_health(tmp_path: Path):
+    runbooks = tmp_path / 'runbooks'
+    reports = tmp_path / 'reports'
+    runbooks.mkdir()
+    reports.mkdir()
+    (runbooks / 'A.md').write_text('# A\n', encoding='utf-8')
+    (reports / 'latest_execution_health.json').write_text(json.dumps({'overall_status': 'warning'}), encoding='utf-8')
+    html = mod.render_index(runbooks, reports)
+    assert 'A.md' in html
+    assert 'warning' in html
diff -ruN '--exclude=.pytest_cache' p5tree/config/docker-compose.execution-p6.override.yml p6tree/config/docker-compose.execution-p6.override.yml
--- p5tree/config/docker-compose.execution-p6.override.yml	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/config/docker-compose.execution-p6.override.yml	2026-03-06 20:13:39.998580415 +0000
@@ -0,0 +1,43 @@
+version: '3.9'
+
+services:
+  alertmanager:
+    volumes:
+      - ./monitoring/alertmanager/trade_execution_alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
+      - ./monitoring/alertmanager/templates:/etc/alertmanager/templates:ro
+
+  grafana:
+    volumes:
+      - ./runbooks:/srv/runbooks:ro
+      - ./var/runbook-reports:/var/lib/trade-runbook/reports:ro
+
+  trade-runbook-server:
+    image: python:3.11-slim
+    container_name: trade-runbook-server
+    restart: unless-stopped
+    working_dir: /srv
+    command: ["python", "/srv/runbooks/server/runbook_server.py"]
+    environment:
+      - RUNBOOK_ROOT=/srv/runbooks
+      - RUNBOOK_REPORT_DIR=/var/lib/trade-runbook/reports
+      - RUNBOOK_SERVER_PORT=${RUNBOOK_SERVER_PORT:-18080}
+    ports:
+      - "${RUNBOOK_SERVER_PORT:-18080}:18080"
+    volumes:
+      - ./runbooks:/srv/runbooks:ro
+      - ./var/runbook-reports:/var/lib/trade-runbook/reports:rw
+
+  # Optional one-shot helper; operators usually run the timer/service outside
+  # compose, but this service is handy for manual backfill in ephemeral envs.
+  trade-execution-backfill:
+    build:
+      context: .
+      dockerfile: python-worker/Dockerfile
+    image: scanner-python-worker:latest
+    profiles: ["ops"]
+    working_dir: /app
+    environment:
+      - REDIS_URL=${REDIS_URL:-redis://redis-worker-1:6379/0}
+      - EXEC_STREAM=${EXEC_STREAM:-orders:exec}
+      - EXECUTION_JOURNAL_DSN=${EXECUTION_JOURNAL_DSN:-}
+    command: ["python", "/app/scripts/backfill_execution_journal_from_orders_exec.py", "--stream", "${EXEC_STREAM:-orders:exec}"]
diff -ruN '--exclude=.pytest_cache' p5tree/config/env.execution-p6.example p6tree/config/env.execution-p6.example
--- p5tree/config/env.execution-p6.example	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/config/env.execution-p6.example	2026-03-06 20:13:39.998778734 +0000
@@ -0,0 +1,15 @@
+# P6 runbook/ops settings
+RUNBOOK_SERVER_PORT=18080
+RUNBOOK_REPORT_DIR=./var/runbook-reports
+
+# Consistency checker thresholds
+EXEC_CONSISTENCY_STREAM_COUNT=20000
+EXEC_CONSISTENCY_CRITICAL_THRESHOLD=1
+EXEC_CONSISTENCY_WARNING_THRESHOLD=10
+USER_STREAM_STALE_MS=120000
+
+# Alertmanager routes (replace placeholders)
+ALERT_ROUTER_DEFAULT_URL=http://alert-router.default.svc.cluster.local/default
+ALERT_ROUTER_CRITICAL_URL=http://alert-router.default.svc.cluster.local/critical
+ALERT_ROUTER_OPS_URL=http://alert-router.default.svc.cluster.local/ops
+ALERT_ROUTER_RISK_URL=http://alert-router.default.svc.cluster.local/risk
diff -ruN '--exclude=.pytest_cache' p5tree/deploy/systemd/README.md p6tree/deploy/systemd/README.md
--- p5tree/deploy/systemd/README.md	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/deploy/systemd/README.md	2026-03-06 20:13:39.998398221 +0000
@@ -0,0 +1,16 @@
+# P6 systemd units
+
+Install the files in this directory under `/etc/systemd/system/` and provide an
+environment file at `/etc/trade/trade-execution.env`.
+
+Recommended enablement:
+
+```bash
+sudo systemctl daemon-reload
+sudo systemctl enable --now trade-execution-healthcheck.timer
+sudo systemctl enable --now trade-execution-backfill.timer
+sudo systemctl enable --now trade-runbook-server.service
+```
+
+The healthcheck timer writes JSON reports into `${RUNBOOK_REPORT_DIR}` so the
+static runbook server can display the latest consistency status.
diff -ruN '--exclude=.pytest_cache' p5tree/deploy/systemd/trade-execution-backfill.service p6tree/deploy/systemd/trade-execution-backfill.service
--- p5tree/deploy/systemd/trade-execution-backfill.service	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/deploy/systemd/trade-execution-backfill.service	2026-03-06 20:13:39.997478620 +0000
@@ -0,0 +1,15 @@
+[Unit]
+Description=Trade execution journal backfill from Redis orders:exec
+After=network-online.target
+Wants=network-online.target
+
+[Service]
+Type=oneshot
+User=trade
+Group=trade
+WorkingDirectory=/opt/trade
+EnvironmentFile=-/etc/trade/trade-execution.env
+ExecStart=/usr/bin/python3 /opt/trade/scripts/backfill_execution_journal_from_orders_exec.py --stream ${EXEC_STREAM:-orders:exec}
+Nice=10
+IOSchedulingClass=best-effort
+IOSchedulingPriority=7
diff -ruN '--exclude=.pytest_cache' p5tree/deploy/systemd/trade-execution-backfill.timer p6tree/deploy/systemd/trade-execution-backfill.timer
--- p5tree/deploy/systemd/trade-execution-backfill.timer	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/deploy/systemd/trade-execution-backfill.timer	2026-03-06 20:13:39.997636165 +0000
@@ -0,0 +1,11 @@
+[Unit]
+Description=Periodic trade execution journal backfill
+
+[Timer]
+OnCalendar=*:0/15
+AccuracySec=30s
+Persistent=true
+Unit=trade-execution-backfill.service
+
+[Install]
+WantedBy=timers.target
diff -ruN '--exclude=.pytest_cache' p5tree/deploy/systemd/trade-execution-healthcheck.service p6tree/deploy/systemd/trade-execution-healthcheck.service
--- p5tree/deploy/systemd/trade-execution-healthcheck.service	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/deploy/systemd/trade-execution-healthcheck.service	2026-03-06 20:13:39.997803115 +0000
@@ -0,0 +1,13 @@
+[Unit]
+Description=Trade execution SQL/Redis consistency healthcheck
+After=network-online.target
+Wants=network-online.target
+
+[Service]
+Type=oneshot
+User=trade
+Group=trade
+WorkingDirectory=/opt/trade
+EnvironmentFile=-/etc/trade/trade-execution.env
+ExecStart=/usr/bin/python3 /opt/trade/scripts/execution_healthcheck.py --report-dir ${RUNBOOK_REPORT_DIR:-/var/lib/trade-runbook/reports}
+SuccessExitStatus=0 1 2
diff -ruN '--exclude=.pytest_cache' p5tree/deploy/systemd/trade-execution-healthcheck.timer p6tree/deploy/systemd/trade-execution-healthcheck.timer
--- p5tree/deploy/systemd/trade-execution-healthcheck.timer	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/deploy/systemd/trade-execution-healthcheck.timer	2026-03-06 20:13:39.998014545 +0000
@@ -0,0 +1,12 @@
+[Unit]
+Description=Periodic trade execution healthcheck
+
+[Timer]
+OnBootSec=2m
+OnUnitActiveSec=1m
+AccuracySec=15s
+Persistent=true
+Unit=trade-execution-healthcheck.service
+
+[Install]
+WantedBy=timers.target
diff -ruN '--exclude=.pytest_cache' p5tree/deploy/systemd/trade-runbook-server.service p6tree/deploy/systemd/trade-runbook-server.service
--- p5tree/deploy/systemd/trade-runbook-server.service	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/deploy/systemd/trade-runbook-server.service	2026-03-06 20:13:39.998248987 +0000
@@ -0,0 +1,17 @@
+[Unit]
+Description=Static trade runbook server
+After=network-online.target
+Wants=network-online.target
+
+[Service]
+Type=simple
+User=trade
+Group=trade
+WorkingDirectory=/opt/trade
+EnvironmentFile=-/etc/trade/trade-execution.env
+ExecStart=/usr/bin/python3 /opt/trade/runbooks/server/runbook_server.py
+Restart=always
+RestartSec=2
+
+[Install]
+WantedBy=multi-user.target
diff -ruN '--exclude=.pytest_cache' p5tree/monitoring/alertmanager/templates/trade_execution.tmpl p6tree/monitoring/alertmanager/templates/trade_execution.tmpl
--- p5tree/monitoring/alertmanager/templates/trade_execution.tmpl	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/monitoring/alertmanager/templates/trade_execution.tmpl	2026-03-06 20:13:39.997287742 +0000
@@ -0,0 +1,27 @@
+{{ define "trade.subject" -}}
+[{{ .Status | toUpper }}][{{ .CommonLabels.severity }}] {{ .CommonLabels.alertname }} {{ if .CommonLabels.symbol }}{{ .CommonLabels.symbol }}{{ end }}
+{{- end }}
+
+{{ define "trade.text" -}}
+Status: {{ .Status }}
+Alert: {{ .CommonLabels.alertname }}
+Severity: {{ .CommonLabels.severity }}
+{{- if .CommonLabels.symbol }}
+Symbol: {{ .CommonLabels.symbol }}
+{{- end }}
+{{- if .CommonLabels.cluster }}
+Cluster: {{ .CommonLabels.cluster }}
+{{- end }}
+
+{{ range .Alerts }}
+- {{ .Annotations.summary }}
+  {{ .Annotations.description }}
+{{ end }}
+
+Runbooks: http://trade-runbook-server:18080/
+Latest health JSON: http://trade-runbook-server:18080/api/health/latest
+{{- end }}
+
+{{ define "trade.html" -}}
+<pre>{{ template "trade.text" . }}</pre>
+{{- end }}
diff -ruN '--exclude=.pytest_cache' p5tree/monitoring/alertmanager/trade_execution_alertmanager.yml p6tree/monitoring/alertmanager/trade_execution_alertmanager.yml
--- p5tree/monitoring/alertmanager/trade_execution_alertmanager.yml	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/monitoring/alertmanager/trade_execution_alertmanager.yml	2026-03-06 20:13:39.997103588 +0000
@@ -0,0 +1,63 @@
+global:
+  resolve_timeout: 5m
+
+templates:
+  - /etc/alertmanager/templates/*.tmpl
+
+route:
+  group_by: ['alertname', 'symbol', 'cluster', 'severity']
+  group_wait: 30s
+  group_interval: 5m
+  repeat_interval: 2h
+  receiver: trade-default
+  routes:
+    - matchers:
+        - severity="crit"
+      receiver: trade-critical
+      continue: false
+    - matchers:
+        - alertname=~"TradeDQ.*|TradeExecutionJournalWriteFailures|TradeExecutionReconcileBacklog"
+      receiver: trade-ops
+      continue: false
+    - matchers:
+        - alertname=~"TradePortfolio.*"
+      receiver: trade-risk
+      continue: false
+
+inhibit_rules:
+  - source_matchers:
+      - severity="crit"
+    target_matchers:
+      - severity="warn"
+    equal: ['alertname', 'symbol', 'cluster']
+
+receivers:
+  - name: trade-default
+    webhook_configs:
+      - url: http://alert-router.default.svc.cluster.local/default
+        send_resolved: true
+
+  - name: trade-critical
+    webhook_configs:
+      - url: http://alert-router.default.svc.cluster.local/critical
+        send_resolved: true
+    email_configs:
+      - to: trade-critical@example.invalid
+        from: alertmanager@example.invalid
+        smarthost: smtp.example.invalid:587
+        auth_username: alertmanager
+        auth_password: change-me
+        require_tls: true
+        headers:
+          Subject: '{{ template "trade.subject" . }}'
+        html: '{{ template "trade.html" . }}'
+
+  - name: trade-ops
+    webhook_configs:
+      - url: http://alert-router.default.svc.cluster.local/ops
+        send_resolved: true
+
+  - name: trade-risk
+    webhook_configs:
+      - url: http://alert-router.default.svc.cluster.local/risk
+        send_resolved: true
diff -ruN '--exclude=.pytest_cache' p5tree/runbooks/server/runbook_server.py p6tree/runbooks/server/runbook_server.py
--- p5tree/runbooks/server/runbook_server.py	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/runbooks/server/runbook_server.py	2026-03-06 20:15:11.216785377 +0000
@@ -0,0 +1,131 @@
+#!/usr/bin/env python3
+from __future__ import annotations
+
+"""Tiny static runbook/health server.
+
+Serves Markdown runbooks, generated health reports, and a lightweight index page
+without introducing a separate Node/Nginx dependency. This makes it easy to run
+inside compose or under systemd on a jump host.
+"""
+
+import html
+import http.server
+import json
+import os
+from pathlib import Path
+from typing import Iterable, List, Tuple
+from urllib.parse import urlparse
+
+
+def discover_runbooks(root: Path) -> List[Path]:
+    return sorted(p for p in root.rglob('*.md') if p.is_file())
+
+
+def render_index(runbook_root: Path, report_root: Path) -> str:
+    reports = sorted(report_root.glob('*.json')) if report_root.exists() else []
+    runbooks = discover_runbooks(runbook_root)
+    items = []
+    for rb in runbooks:
+        rel = rb.relative_to(runbook_root)
+        items.append(f'<li><a href="/runbooks/{html.escape(str(rel))}">{html.escape(str(rel))}</a></li>')
+    report_items = []
+    for rp in reports:
+        report_items.append(f'<li><a href="/reports/{html.escape(rp.name)}">{html.escape(rp.name)}</a></li>')
+    latest = report_root / 'latest_execution_health.json'
+    badge = 'unknown'
+    if latest.exists():
+        try:
+            badge = json.loads(latest.read_text(encoding='utf-8')).get('overall_status', 'unknown')
+        except Exception:
+            badge = 'invalid'
+    return f"""<!doctype html>
+<html><head><meta charset="utf-8"><title>Trade Runbooks</title>
+<style>body{{font-family:sans-serif;margin:2rem;max-width:1100px}}code{{background:#f4f4f4;padding:.1rem .3rem}}.badge{{padding:.15rem .5rem;border-radius:4px;background:#eee}}.ok{{background:#d1fae5}}.warning{{background:#fef3c7}}.critical{{background:#fee2e2}}</style>
+</head><body>
+<h1>Trade Execution Runbooks</h1>
+<p>Latest health: <span class="badge {badge}">{html.escape(badge)}</span> — <a href="/api/health/latest">JSON</a></p>
+<h2>Runbooks</h2><ul>{''.join(items) or '<li>no runbooks found</li>'}</ul>
+<h2>Reports</h2><ul>{''.join(report_items) or '<li>no reports found</li>'}</ul>
+<p><code>/healthz</code> returns 200 if the server process is alive.</p>
+</body></html>"""
+
+
+class Handler(http.server.SimpleHTTPRequestHandler):
+    runbook_root = Path(os.getenv('RUNBOOK_ROOT', str(Path(__file__).resolve().parents[1])))
+    report_root = Path(os.getenv('RUNBOOK_REPORT_DIR', '/var/lib/trade-runbook/reports'))
+
+    def do_GET(self):
+        parsed = urlparse(self.path)
+        if parsed.path == '/healthz':
+            body = b'ok\n'
+            self.send_response(200)
+            self.send_header('Content-Type', 'text/plain; charset=utf-8')
+            self.send_header('Content-Length', str(len(body)))
+            self.end_headers()
+            self.wfile.write(body)
+            return
+        if parsed.path == '/api/health/latest':
+            latest = self.report_root / 'latest_execution_health.json'
+            if latest.exists():
+                body = latest.read_bytes()
+                self.send_response(200)
+                self.send_header('Content-Type', 'application/json; charset=utf-8')
+                self.send_header('Content-Length', str(len(body)))
+                self.end_headers()
+                self.wfile.write(body)
+                return
+            self.send_error(404, 'health report not found')
+            return
+        if parsed.path == '/' or parsed.path == '/index.html':
+            body = render_index(self.runbook_root, self.report_root).encode('utf-8')
+            self.send_response(200)
+            self.send_header('Content-Type', 'text/html; charset=utf-8')
+            self.send_header('Content-Length', str(len(body)))
+            self.end_headers()
+            self.wfile.write(body)
+            return
+        if parsed.path.startswith('/runbooks/'):
+            rel = parsed.path[len('/runbooks/'):]
+            target = (self.runbook_root / rel).resolve()
+            if not str(target).startswith(str(self.runbook_root.resolve())) or not target.exists():
+                self.send_error(404)
+                return
+            body = target.read_bytes()
+            ctype = 'text/markdown; charset=utf-8' if target.suffix.lower() == '.md' else 'application/octet-stream'
+            self.send_response(200)
+            self.send_header('Content-Type', ctype)
+            self.send_header('Content-Length', str(len(body)))
+            self.end_headers()
+            self.wfile.write(body)
+            return
+        if parsed.path.startswith('/reports/'):
+            rel = parsed.path[len('/reports/'):]
+            target = (self.report_root / rel).resolve()
+            if not str(target).startswith(str(self.report_root.resolve())) or not target.exists():
+                self.send_error(404)
+                return
+            body = target.read_bytes()
+            self.send_response(200)
+            self.send_header('Content-Type', 'application/json; charset=utf-8')
+            self.send_header('Content-Length', str(len(body)))
+            self.end_headers()
+            self.wfile.write(body)
+            return
+        self.send_error(404)
+
+
+def main() -> int:
+    host = os.getenv('RUNBOOK_SERVER_BIND', '0.0.0.0')
+    port = int(os.getenv('RUNBOOK_SERVER_PORT', '18080'))
+    server = http.server.ThreadingHTTPServer((host, port), Handler)
+    try:
+        server.serve_forever()
+    except KeyboardInterrupt:
+        pass
+    finally:
+        server.server_close()
+    return 0
+
+
+if __name__ == '__main__':  # pragma: no cover
+    raise SystemExit(main())
diff -ruN '--exclude=.pytest_cache' p5tree/scripts/check_execution_consistency.py p6tree/scripts/check_execution_consistency.py
--- p5tree/scripts/check_execution_consistency.py	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/scripts/check_execution_consistency.py	2026-03-06 20:14:35.300179421 +0000
@@ -0,0 +1,293 @@
+#!/usr/bin/env python3
+from __future__ import annotations
+
+"""Cross-check Redis execution state against Redis orders:exec and SQL journal.
+
+Purpose
+-------
+P6 introduces an operator-friendly consistency checker that can be run from a
+systemd timer, cron, or manually during incidents. The checker compares three
+storage layers that may diverge during partial rollouts or transient failures:
+
+* Redis state snapshots: ``orders:state:*``
+* Redis execution facts: ``orders:exec`` stream
+* SQL durable mirror: ``execution_orders`` / ``execution_protection_refs``
+
+The checker is intentionally deterministic and read-only. It produces a compact
+JSON summary, writes an optional report file consumed by the runbook server, and
+returns a non-zero code only when configured critical thresholds are exceeded.
+"""
+
+import argparse
+import json
+import os
+import time
+from dataclasses import dataclass, asdict
+from typing import Any, Dict, Iterable, Iterator, List, Mapping, Optional, Tuple
+
+
+STATE_FIELDS = ("symbol", "status", "fsm_state", "execution_policy", "position_side")
+PROTECTION_FIELDS = (
+    "sl_algo_id",
+    "tp1_algo_id",
+    "tp2_algo_id",
+    "tp3_algo_id",
+    "trail_algo_id",
+)
+
+
+def _i(v: Any, default: int = 0) -> int:
+    try:
+        if v is None:
+            return default
+        return int(float(v))
+    except Exception:
+        return int(default)
+
+
+def _s(v: Any) -> str:
+    return "" if v is None else str(v)
+
+
+def _loads(value: Any) -> Dict[str, Any]:
+    if value is None:
+        return {}
+    if isinstance(value, dict):
+        return dict(value)
+    if isinstance(value, bytes):
+        value = value.decode('utf-8', 'replace')
+    try:
+        obj = json.loads(value)
+        return obj if isinstance(obj, dict) else {}
+    except Exception:
+        return {}
+
+
+@dataclass(frozen=True)
+class ConsistencyMismatch:
+    sid: str
+    severity: str
+    category: str
+    detail: str
+
+
+@dataclass(frozen=True)
+class ConsistencySummary:
+    checked_at_ms: int
+    redis_state_count: int
+    stream_sid_count: int
+    sql_order_count: int
+    mismatches_total: int
+    critical_mismatches: int
+    warning_mismatches: int
+    mismatches: List[Dict[str, Any]]
+
+
+class RedisExecutionReader:
+    def __init__(self, redis_client: Any, state_prefix: str, exec_stream: str):
+        self.redis = redis_client
+        self.state_prefix = state_prefix
+        self.exec_stream = exec_stream
+
+    def iter_state(self) -> Iterator[Tuple[str, Dict[str, Any]]]:
+        for key in self.redis.scan_iter(match=f"{self.state_prefix}*"):
+            doc = _loads(self.redis.get(key))
+            sid = _s(doc.get('sid')) or _s(key).split(self.state_prefix, 1)[-1]
+            if sid:
+                yield sid, doc
+
+    def latest_stream_events(self, count: int = 20000) -> Dict[str, Dict[str, Any]]:
+        latest: Dict[str, Dict[str, Any]] = {}
+        rows = self.redis.xrevrange(self.exec_stream, '+', '-', count=count)
+        # xrevrange returns newest-first. Keep first seen per sid as latest.
+        for stream_id, fields in rows:
+            payload = {str(k): v for k, v in dict(fields or {}).items()}
+            sid = _s(payload.get('sid'))
+            if sid and sid not in latest:
+                payload['stream_id'] = stream_id
+                latest[sid] = payload
+        return latest
+
+
+class SQLExecutionReader:
+    def __init__(self, conn: Any):
+        self.conn = conn
+
+    def load_orders(self) -> Dict[str, Dict[str, Any]]:
+        sql = (
+            "SELECT sid, symbol, action, status, fsm_state, execution_policy, position_side, state_jsonb "
+            "FROM execution_orders"
+        )
+        out: Dict[str, Dict[str, Any]] = {}
+        with self.conn.cursor() as cur:
+            cur.execute(sql)
+            for sid, symbol, action, status, fsm_state, execution_policy, position_side, state_jsonb in cur.fetchall():
+                doc = _loads(state_jsonb)
+                doc.setdefault('sid', sid)
+                doc.setdefault('symbol', symbol)
+                doc.setdefault('action', action)
+                doc.setdefault('status', status)
+                doc.setdefault('fsm_state', fsm_state)
+                doc.setdefault('execution_policy', execution_policy)
+                doc.setdefault('position_side', position_side)
+                out[_s(sid)] = doc
+        return out
+
+    def load_protection_refs(self) -> Dict[str, Dict[str, Any]]:
+        sql = (
+            "SELECT sid, symbol, sl_algo_id, tp1_algo_id, tp2_algo_id, tp3_algo_id, trail_algo_id "
+            "FROM execution_protection_refs"
+        )
+        out: Dict[str, Dict[str, Any]] = {}
+        with self.conn.cursor() as cur:
+            cur.execute(sql)
+            for sid, symbol, sl_algo_id, tp1_algo_id, tp2_algo_id, tp3_algo_id, trail_algo_id in cur.fetchall():
+                out[_s(sid)] = {
+                    'sid': sid,
+                    'symbol': symbol,
+                    'sl_algo_id': sl_algo_id,
+                    'tp1_algo_id': tp1_algo_id,
+                    'tp2_algo_id': tp2_algo_id,
+                    'tp3_algo_id': tp3_algo_id,
+                    'trail_algo_id': trail_algo_id,
+                }
+        return out
+
+
+def compare_execution_views(
+    redis_state: Mapping[str, Mapping[str, Any]],
+    stream_latest: Mapping[str, Mapping[str, Any]],
+    sql_orders: Mapping[str, Mapping[str, Any]],
+    sql_refs: Optional[Mapping[str, Mapping[str, Any]]] = None,
+) -> List[ConsistencyMismatch]:
+    sql_refs = sql_refs or {}
+    mismatches: List[ConsistencyMismatch] = []
+    all_sids = set(redis_state) | set(stream_latest) | set(sql_orders)
+    for sid in sorted(all_sids):
+        r = dict(redis_state.get(sid) or {})
+        s = dict(stream_latest.get(sid) or {})
+        q = dict(sql_orders.get(sid) or {})
+        qr = dict(sql_refs.get(sid) or {})
+        present_count = int(bool(r)) + int(bool(s)) + int(bool(q))
+        if present_count < 2:
+            mismatches.append(ConsistencyMismatch(sid, 'warning', 'presence', 'sid missing from at least two mirrors'))
+            continue
+        if not r:
+            mismatches.append(ConsistencyMismatch(sid, 'warning', 'redis_state_missing', 'Redis orders:state entry is missing'))
+        if not s:
+            mismatches.append(ConsistencyMismatch(sid, 'warning', 'stream_missing', 'Redis orders:exec latest event not found in scan window'))
+        if not q:
+            mismatches.append(ConsistencyMismatch(sid, 'critical', 'sql_missing', 'SQL execution_orders row is missing'))
+        for field in STATE_FIELDS:
+            values = {source: _s(doc.get(field)) for source, doc in [('redis', r), ('stream', s), ('sql', q)] if doc}
+            if len(set(v for v in values.values() if v != '')) > 1:
+                mismatches.append(ConsistencyMismatch(
+                    sid,
+                    'critical' if field in {'status', 'fsm_state'} else 'warning',
+                    f'{field}_mismatch',
+                    ', '.join(f'{k}={v}' for k, v in sorted(values.items())),
+                ))
+        # Protection refs are allowed to be absent until protection arming begins.
+        protection_values = {}
+        for field in PROTECTION_FIELDS:
+            vals = {}
+            if r:
+                vals['redis'] = _s(r.get(field))
+            if q:
+                vals['sql_order'] = _s(q.get(field))
+            if qr:
+                vals['sql_refs'] = _s(qr.get(field))
+            nz = {k: v for k, v in vals.items() if v not in {'', '0', 'None'}}
+            if len(set(nz.values())) > 1:
+                mismatches.append(ConsistencyMismatch(
+                    sid,
+                    'warning',
+                    f'{field}_mismatch',
+                    ', '.join(f'{k}={v}' for k, v in sorted(nz.items())),
+                ))
+                protection_values[field] = nz
+    return mismatches
+
+
+def summarise_mismatches(redis_state_count: int, stream_sid_count: int, sql_order_count: int, mismatches: Iterable[ConsistencyMismatch]) -> ConsistencySummary:
+    items = list(mismatches)
+    critical = sum(1 for m in items if m.severity == 'critical')
+    warning = sum(1 for m in items if m.severity != 'critical')
+    return ConsistencySummary(
+        checked_at_ms=int(time.time() * 1000),
+        redis_state_count=redis_state_count,
+        stream_sid_count=stream_sid_count,
+        sql_order_count=sql_order_count,
+        mismatches_total=len(items),
+        critical_mismatches=critical,
+        warning_mismatches=warning,
+        mismatches=[asdict(m) for m in items],
+    )
+
+
+def _connect_redis():
+    import redis  # type: ignore
+    return redis.from_url(os.getenv('REDIS_URL', 'redis://localhost:6379/0'), decode_responses=True)
+
+
+def _connect_pg(dsn: str):
+    try:
+        import psycopg  # type: ignore
+        return psycopg.connect(dsn)
+    except Exception:  # pragma: no cover
+        import psycopg2  # type: ignore
+        return psycopg2.connect(dsn)
+
+
+def run_check(*, redis_url: str, journal_dsn: str, state_prefix: str, exec_stream: str, stream_count: int = 20000) -> ConsistencySummary:
+    import redis  # type: ignore
+    r = redis.from_url(redis_url, decode_responses=True)
+    redis_reader = RedisExecutionReader(r, state_prefix, exec_stream)
+    redis_state = dict(redis_reader.iter_state())
+    stream_latest = redis_reader.latest_stream_events(count=stream_count)
+    conn = _connect_pg(journal_dsn)
+    sql_reader = SQLExecutionReader(conn)
+    sql_orders = sql_reader.load_orders()
+    sql_refs = sql_reader.load_protection_refs()
+    mismatches = compare_execution_views(redis_state, stream_latest, sql_orders, sql_refs)
+    return summarise_mismatches(len(redis_state), len(stream_latest), len(sql_orders), mismatches)
+
+
+def main(argv: Optional[List[str]] = None) -> int:
+    parser = argparse.ArgumentParser(description='Check consistency between Redis orders state/stream and SQL execution journal.')
+    parser.add_argument('--redis-url', default=os.getenv('REDIS_URL', 'redis://localhost:6379/0'))
+    parser.add_argument('--journal-dsn', default=os.getenv('EXECUTION_JOURNAL_DSN', ''))
+    parser.add_argument('--state-prefix', default=os.getenv('ORDERS_STATE_KEY_PREFIX', 'orders:state:'))
+    parser.add_argument('--exec-stream', default=os.getenv('EXEC_STREAM', 'orders:exec'))
+    parser.add_argument('--stream-count', type=int, default=int(os.getenv('EXEC_CONSISTENCY_STREAM_COUNT', '20000')))
+    parser.add_argument('--report-path', default=os.getenv('EXEC_CONSISTENCY_REPORT_PATH', ''))
+    parser.add_argument('--critical-threshold', type=int, default=int(os.getenv('EXEC_CONSISTENCY_CRITICAL_THRESHOLD', '1')))
+    parser.add_argument('--warning-threshold', type=int, default=int(os.getenv('EXEC_CONSISTENCY_WARNING_THRESHOLD', '10')))
+    args = parser.parse_args(argv)
+
+    if not args.journal_dsn:
+        raise SystemExit('EXECUTION_JOURNAL_DSN/--journal-dsn is required')
+
+    summary = run_check(
+        redis_url=args.redis_url,
+        journal_dsn=args.journal_dsn,
+        state_prefix=args.state_prefix,
+        exec_stream=args.exec_stream,
+        stream_count=args.stream_count,
+    )
+    payload = asdict(summary)
+    text = json.dumps(payload, ensure_ascii=False, indent=2, sort_keys=True)
+    print(text)
+    if args.report_path:
+        os.makedirs(os.path.dirname(args.report_path), exist_ok=True)
+        with open(args.report_path, 'w', encoding='utf-8') as fh:
+            fh.write(text + '\n')
+    if summary.critical_mismatches >= args.critical_threshold:
+        return 2
+    if summary.warning_mismatches >= args.warning_threshold:
+        return 1
+    return 0
+
+
+if __name__ == '__main__':  # pragma: no cover
+    raise SystemExit(main())
diff -ruN '--exclude=.pytest_cache' p5tree/scripts/execution_healthcheck.py p6tree/scripts/execution_healthcheck.py
--- p5tree/scripts/execution_healthcheck.py	1970-01-01 00:00:00.000000000 +0000
+++ p6tree/scripts/execution_healthcheck.py	2026-03-06 20:13:39.996622826 +0000
@@ -0,0 +1,87 @@
+#!/usr/bin/env python3
+from __future__ import annotations
+
+"""Periodic execution healthcheck for systemd timer / cron.
+
+Runs the SQL/Redis consistency checker and a small set of freshness checks,
+writes a stable JSON document consumed by the runbook server, and exits with a
+severity-driven code suitable for timer monitoring.
+"""
+
+import argparse
+import json
+import os
+import sys
+import time
+from pathlib import Path
+from typing import Any, Dict, List, Optional
+
+SCRIPT_DIR = Path(__file__).resolve().parent
+if str(SCRIPT_DIR) not in sys.path:
+    sys.path.insert(0, str(SCRIPT_DIR))
+
+import check_execution_consistency as consistency
+
+
+def _check_user_stream_freshness(redis_client: Any, *, cache_prefix: str, stale_ms: int) -> Dict[str, Any]:
+    newest = 0
+    keys_checked = 0
+    for key in redis_client.scan_iter(match=f"{cache_prefix}*"):
+        keys_checked += 1
+        try:
+            doc = redis_client.hgetall(key) or {}
+        except Exception:
+            doc = {}
+        newest = max(newest, int(float(doc.get('event_time_ms') or 0)))
+    now_ms = int(time.time() * 1000)
+    age_ms = max(0, now_ms - newest) if newest else 10**12
+    return {
+        'keys_checked': keys_checked,
+        'newest_event_time_ms': newest,
+        'age_ms': age_ms,
+        'is_stale': bool(age_ms > stale_ms),
+    }
+
+
+def main(argv: Optional[List[str]] = None) -> int:
+    parser = argparse.ArgumentParser(description='Run execution health checks and write JSON report.')
+    parser.add_argument('--redis-url', default=os.getenv('REDIS_URL', 'redis://localhost:6379/0'))
+    parser.add_argument('--journal-dsn', default=os.getenv('EXECUTION_JOURNAL_DSN', ''))
+    parser.add_argument('--report-dir', default=os.getenv('RUNBOOK_REPORT_DIR', '/var/lib/trade-runbook/reports'))
+    parser.add_argument('--user-stream-cache-prefix', default=os.getenv('USER_STREAM_CACHE_PREFIX', 'orders:user_stream:'))
+    parser.add_argument('--user-stream-stale-ms', type=int, default=int(os.getenv('USER_STREAM_STALE_MS', '120000')))
+    args = parser.parse_args(argv)
+
+    import redis  # type: ignore
+    r = redis.from_url(args.redis_url, decode_responses=True)
+
+    summary = consistency.run_check(
+        redis_url=args.redis_url,
+        journal_dsn=args.journal_dsn,
+        state_prefix=os.getenv('ORDERS_STATE_KEY_PREFIX', 'orders:state:'),
+        exec_stream=os.getenv('EXEC_STREAM', 'orders:exec'),
+        stream_count=int(os.getenv('EXEC_CONSISTENCY_STREAM_COUNT', '20000')),
+    )
+    freshness = _check_user_stream_freshness(r, cache_prefix=args.user_stream_cache_prefix, stale_ms=args.user_stream_stale_ms)
+
+    report = {
+        'checked_at_ms': int(time.time() * 1000),
+        'consistency': consistency.asdict(summary),
+        'user_stream': freshness,
+        'overall_status': 'critical' if summary.critical_mismatches > 0 else ('warning' if summary.warning_mismatches > 0 or freshness['is_stale'] else 'ok'),
+    }
+    report_dir = Path(args.report_dir)
+    report_dir.mkdir(parents=True, exist_ok=True)
+    latest = report_dir / 'latest_execution_health.json'
+    latest.write_text(json.dumps(report, ensure_ascii=False, indent=2, sort_keys=True) + '
+', encoding='utf-8')
+    print(json.dumps(report, ensure_ascii=False, sort_keys=True))
+    if report['overall_status'] == 'critical':
+        return 2
+    if report['overall_status'] == 'warning':
+        return 1
+    return 0
+
+
+if __name__ == '__main__':  # pragma: no cover
+    raise SystemExit(main())
